# ABC460 解法レポート

**日付**: 2026年05月30日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC460](https://atcoder.jp/contests/abc460)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題B: B - Two RingsEditorial

### 問題文

xy
平面上に
2
つの円
C_1, C_2
があります。ただし、本問題において円とは円周のことを指します。
円
C_1
は中心の座標が
(X_1, Y_1)
で半径が
R_1
です。
円
C_2
は中心の座標が
(X_2, Y_2)
で半径が
R_2
です。
円
C_1
と円
C_2
が共有点を持つかどうかを判定してください。言い換えると、点
(X_1, Y_1)
からの距離が
R_1
であり、かつ点
(X_2, Y_2)
からの距離が
R_2
であるような点が
1
個以上存在するかどうかを判定してください。
T
個のテストケースが与えられるのでそれぞれについて答えを求めてください。

### 制約

1 \leq T \leq 100
0 \leq X_1, Y_1, X_2, Y_2 \leq 10^9
1 \leq R_1, R_2 \leq 10^9
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。ここで
\mathrm{case}_i
は
i
番目のテストケースを意味する。
T
\mathrm{case}_1
\mathrm{case}_2
\vdots
\mathrm{case}_T
各テストケースは以下の形式で与えられる。
X_1
Y_1
R_1
X_2
Y_2
R_2

### 出力

T
行出力せよ。
i
行目には
i
番目のテストケースの答えを出力せよ。
各テストケースでは、円
C_1
と円
C_2
が共有点を持つ場合は
Yes
を、そうでない場合は
No
を出力せよ。

### 入力例 1

7
0 0 2 2 3 2
0 0 2 2 3 1
1 2 5 3 2 1
5 4 2 8 8 3
2 1 5 5 1 2
0 0 1 0 0 1
0 0 500000000 1 1000000000 500000000

### 出力例 1

Yes
No
No
Yes
Yes
Yes
No
例えば
1
番目のテストケースでは、
C_1
と
C_2
の位置関係は以下の図のようになります。

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 48 ms |
| 提出リンク | [#76272854](https://atcoder.jp/contests/abc460/submissions/76272854) |

<details><summary>提出コード</summary>

```python
import math
T = int(input())

def judge(x1,y1,r1,x2,y2,r2):
  d = ((x2-x1)**2 + (y2-y1)**2)
  if d < (r1-r2)**2:
    return "No"
  elif d == (r1-r2)**2 or (r1-r2)**2 < d < (r1+r2)**2 or d == (r1+r2)**2:
    return "Yes"
  elif (r1+r2)**2< d:
    return "No"

for i in range(T):
  x1,y1,r1,x2,y2,r2 = map(int,input().split())
  result = judge(x1,y1,r1,x2,y2,r2)
  print(result)
  
```

</details>

---

### 解説

## アルゴリズム解説

この問題では、xy平面上に2つの円 $C_1$（中心 $(X_1, Y_1)$、半径 $R_1$）と $C_2$（中心 $(X_2, Y_2)$、半径 $R_2$）が与えられ、これらが共有点を持つかどうかを判定します。

2つの円の共有点の有無は、**2つの円の中心間の距離 $d$** と **それぞれの半径 $R_1, R_2$** の関係によって決まります。

1.  **中心間の距離 $d$ の計算:**
    まず、2つの円の中心 $(X_1, Y_1)$ と $(X_2, Y_2)$ の間の距離 $d$ を求めます。これは三平方の定理により、
    $d = \sqrt{(X_2 - X_1)^2 + (Y_2 - Y_1)^2}$
    で計算できます。
    しかし、平方根 (`sqrt`) を用いると浮動小数点数になり、計算誤差が生じる可能性があります。競技プログラミングではこのような誤差は避けるべきです。そこで、比較対象である半径の和や差も2乗することで、全て整数演算で比較できるようにします。つまり、`d` ではなく `d^2` を用いて比較します。
    $d^2 = (X_2 - X_1)^2 + (Y_2 - Y_1)^2$

2.  **共有点の有無の判定:**
    $d^2$ と $(R_1 + R_2)^2$（半径の和の2乗）、そして $(R_1 - R_2)^2$（半径の差の2乗、これは $|R_1 - R_2|^2$ と等しい）を比較します。

    *   **ケース1: 円が完全に内包されていて、互いに離れている場合 (No)**
        一方の円がもう一方の円の内部に完全に含まれており、かつ互いに接していない場合です。
        これは、中心間距離 $d$ が半径の差の絶対値 $|R_1 - R_2|$ よりも小さい場合に発生します。
        $d < |R_1 - R_2|$
        2乗して比較すると、 $d^2 < (R_1 - R_2)^2$ となります。
        特殊なケースとして、中心が同じで半径が異なる場合 ($d=0, R_1 \neq R_2$) もこの条件で適切に `No` と判定されます。

    *   **ケース2: 円が外部で完全に離れている場合 (No)**
        2つの円が互いに外部にあり、かつ接していない場合です。
        これは、中心間距離 $d$ が半径の和 $R_1 + R_2$ よりも大きい場合に発生します。
        $d > R_1 + R_2$
        2乗して比較すると、 $d^2 > (R_1 + R_2)^2$ となります。

    *   **ケース3: 共有点を持つ場合 (Yes)**
        上記の2つのケースに当てはまらない場合、共有点を持ちます。これは以下のいずれかの状況です。
        -   **内接:** $d = |R_1 - R_2|$ ($d^2 = (R_1 - R_2)^2$)
        -   **2点で交わる:** $|R_1 - R_2| < d < R_1 + R_2$ ($(R_1 - R_2)^2 < d^2 < (R_1 + R_2)^2$)
        -   **外接:** $d = R_1 + R_2$ ($d^2 = (R_1 + R_2)^2$)
        -   **完全に重なる:** $d = 0$ かつ $R_1 = R_2$ ($d^2 = 0, (R_1 - R_2)^2 = 0$)
        これらの条件は、まとめると $(R_1 - R_2)^2 \le d^2 \le (R_1 + R_2)^2$ となります。

以上のロジックを順番に判定することで、共有点の有無を判断できます。

## 計算量

*   **時間計算量: O(T)**
    各テストケースにおいて、中心間の距離の2乗や半径の和・差の2乗を計算する定数回の算術演算と定数回の比較を行うだけです。したがって、1つのテストケースあたりの計算量は $O(1)$ です。
    テストケースが $T$ 個あるため、全体の時間計算量は $O(T)$ となります。

*   **空間計算量: O(1)**
    少数の変数を保持するだけであり、入力サイズに依存して追加のメモリを使用することはありません。

## Python 模範解答

```python
import math # mathモジュールは使わないので、もしimportされていれば削除しても問題ないです。

def solve():
  x1, y1, r1, x2, y2, r2 = map(int, input().split())

  # 中心間距離の2乗を計算 (d^2)
  # 浮動小数点数誤差を避けるため、距離の平方根を取らずに2乗のまま比較します。
  d_squared = (x2 - x1)**2 + (y2 - y1)**2
  
  # 半径の差の2乗 (|R1 - R2|^2)
  # (R1 - R2)^2 と同じ意味。2乗すると負の値も正になるため、絶対値は不要です。
  r_diff_squared = (r1 - r2)**2
  
  # 半径の和の2乗 ((R1 + R2)^2)
  r_sum_squared = (r1 + r2)**2

  # ケース1: 一方の円がもう一方の円の内側に完全に含まれており、離れている場合
  # 中心間距離が半径の差の絶対値よりも小さい場合
  if d_squared < r_diff_squared:
    print("No")
  # ケース2: 2つの円が外部で完全に離れている場合
  # 中心間距離が半径の和よりも大きい場合
  elif d_squared > r_sum_squared:
    print("No")
  # ケース3: 上記以外の全ての場合 (共有点を持つ)
  # これは、(R1 - R2)^2 <= d_squared <= (R1 + R2)^2 の条件を満たす場合です。
  # 具体的には、内接、外接、2点で交わる、完全に重なるケースが含まれます。
  else:
    print("Yes")

# テストケースの数 T を読み込む
T = int(input())

# T 回のテストケースを処理する
for _ in range(T):
  solve()

```

## 実装のポイント

*   **浮動小数点数誤差の回避:** 中心間の距離を直接 `math.sqrt()` で計算すると、浮動小数点数誤差が発生する可能性があります。AtCoderのような競技プログラミングでは、微小な誤差で不正解となるケースが多いため、可能な限り整数演算で処理するのが鉄則です。この問題では、距離 $d$ と半径の和・差を比較する際に、両辺を2乗することで平方根の計算を回避し、全て整数演算で比較を行っています。
*   **半径の差の2乗:** `(R1 - R2)**2` は、`(|R1 - R2|)**2` と等価です。負の数を2乗しても正になるため、`abs()` 関数を使う必要はありません。
*   **整数のオーバーフロー（Pythonでは考慮不要）:** 座標や半径が $10^9$ になるため、その2乗は $10^{18}$ になります。Pythonの整数型は任意精度なので、このサイズの数値も問題なく扱えます。C++やJavaのような言語では、`long long` 型など64ビット整数型を使用しないとオーバーフローする可能性があるので注意が必要です。
*   **中心が一致する特殊ケース:**
    *   $d=0$ かつ $R_1 = R_2$ の場合（完全に重なる2円）： $d^2=0$, $(R_1-R_2)^2=0$, $(R_1+R_2)^2=(2R_1)^2$ となり、$(R_1-R_2)^2 \le d^2 \le (R_1+R_2)^2$ (すなわち $0 \le 0 \le (2R_1)^2$) が真であるため、`Yes` と判定されます。これは正しいです。
    *   $d=0$ かつ $R_1 \neq R_2$ の場合（中心が同じで半径が異なる2円）： $d^2=0$, $(R_1-R_2)^2 > 0$ となります。このとき、$d^2 < (R_1-R_2)^2$ (すなわち $0 < (R_1-R_2)^2$) が真であるため、`No` と判定されます。これも正しいです。
    このように、提示した判定ロジックは中心が一致する特殊なケースも自然にカバーしています。

---

## 問題C: C - SushiEditorial

### 問題文

寿司の材料として、
N
個のシャリと
M
個のネタがあります。
i
番目のシャリの重さは
A_i
、
j
番目のネタの重さは
B_j
です。
あなたは、シャリとネタを組み合わせることで寿司を作ろうとしています。
寿司を
1
つ作るためには、
1
つのシャリと
1
つのネタを組み合わせる必要があります。ただし、ネタの重さはシャリの重さの
2
倍以下でなければなりません。また、
1
つのシャリやネタを複数の寿司に使うことはできません。
作ることのできる寿司の個数の最大値を求めてください。

### 制約

1 \leq N, M \leq 2 \times 10^5
1 \leq A_i, B_j \leq 10^9
入力される値はすべて整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
M
A_1
A_2
\ldots
A_N
B_1
B_2
\ldots
B_M

### 出力

答えを出力せよ。

### 入力例 1

4 5
4 2 1 8
14 9 3 2 9

### 出力例 1

3
1
番目のシャリと
3
番目のネタ、
2
番目のシャリと
4
番目のネタ、
4
番目のシャリと
1
番目のネタを組み合わせるなどの方法で
3
つの寿司を作ることができます。
4
つ以上の寿司を作ることはできないため、
3
を出力します。

### 入力例 2

3 3
5 5 3
11 1000 1000

### 出力例 2

0

### 入力例 3

8 7
2 3 4 4 4 3 2 3
8 5 5 9 9 7 1

### 出力例 3

5

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **❌ TLE** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | None ms |
| 提出リンク | [#76285530](https://atcoder.jp/contests/abc460/submissions/76285530) |

<details><summary>提出コード</summary>

```python
import bisect

def judge(A,B,N,M):
  count = 0
  for i in range(N):
    select = A[i] * 2
    j = bisect.bisect_right(B,select)
    if j > 0:
      count += 1
      del B[0]
      
  return count

N,M = map(int,input().split())

A = list(map(int,input().split()))
B = list(map(int,input().split()))

A.sort()
B.sort()

print(judge(A,B,N,M))


```

</details>

---

### 解説

## TLE の原因分析

提出されたコードが TLE（時間制限超過）となった主な原因は、Python のリストに対する `del B[0]` 操作の計算量です。

1.  **アルゴリズムの概略:**
    コードは、シャリ `A` とネタ `B` をそれぞれ昇順にソートした後、シャリ `A` を小さいものから順に見ていきます。各シャリ `A[i]` に対して、条件 `B_j <= A[i] * 2` を満たすネタ `B_j` を `bisect.bisect_right` で探し、もしそのようなネタが見つかったら、`B` の中で最も小さいネタ `B[0]` を選び、それを `del B[0]` でリストから削除しています。

2.  **`del B[0]` の計算量:**
    Python のリストは動的配列で実装されており、リストの先頭 (インデックス 0) の要素を削除する `del B[0]` 操作は、残りのすべての要素 (つまり `B[1]` から `B[len(B)-1]`) を一つずつ前にずらす必要があります。この操作の計算量は、リストの現在の長さ `len(B)` に比例し、$O(\text{len(B)})$ となります。

3.  **全体の計算量:**
    `judge` 関数内では、`N` 回のループがあり、それぞれのループ内で `bisect.bisect_right` が $O(\log M')$ (ここで $M'$ はその時点での `B` の長さ) で実行され、その後 `del B[0]` が $O(M')$ で実行される可能性があります。
    最悪の場合、`N` 個のシャリすべてがネタとマッチし、`del B[0]` が `N` 回実行されます。このとき、`M'` は `M` から `M-N+1` まで変化します。
    したがって、`del B[0]` による合計計算量は $O(M + (M-1) + \dots + (M-N+1)) \approx O(N \times M)$ となります。
    問題の制約は $N, M \leq 2 \times 10^5$ ですから、最悪の場合 $N \times M$ は $(2 \times 10^5)^2 = 4 \times 10^{10}$ に達し、これは明らかに時間制限を超過します。

4.  **アルゴリズムの修正点:**
    現在のコードは、貪欲戦略としては概ね正しい方向性（シャリとネタをソートし、小さいものから順に処理する）を取っていますが、リストからの要素削除を効率的に行う必要があります。このような「ソートされた2つのリストから、条件を満たすペアを貪欲に探す」という問題は、一般的に**2ポインタ法**で効率的に解決できます。

## 正解アルゴリズム

この問題は、貪欲法と2ポインタ法で解くことができます。

**何を求めているか:**
作ることのできる寿司の個数の最大値。これは、1つのシャリと1つのネタを1回ずつ使い、ネタの重さがシャリの重さの2倍以下であるという条件を満たすペアの最大数です。

**どの手法で解くべきか:**
シャリとネタをそれぞれ昇順にソートし、2ポインタ法でペアを探します。

**アルゴリズムの詳細:**

1.  **入力のソート:** シャリの重さのリスト `A` とネタの重さのリスト `B` をそれぞれ昇順にソートします。
    *   理由: シャリを小さい方から見ることで、より制約の厳しいシャリ（使えるネタの範囲が狭いシャリ）から先に処理できます。ネタを小さい方から見ることで、各シャリに対して、制約を満たすネタのうち最も軽いものを優先的に使うことができます。これにより、より重いネタを後の（より制約の緩い）シャリのために残すことができ、全体の最大化につながります。

2.  **2ポインタの初期化:**
    *   シャリのリスト `A` を走査するポインタ `shari_ptr` を `0` に初期化します。
    *   ネタのリスト `B` を走査するポインタ `neta_ptr` を `0` に初期化します。
    *   作成できた寿司の数をカウントする変数 `count` を `0` に初期化します。

3.  **ペアリングのループ:**
    `shari_ptr` が `N` 未満かつ `neta_ptr` が `M` 未満の間、以下の処理を繰り返します。

    a.  **条件判定:** 現在のネタ `B[neta_ptr]` が、現在のシャリ `A[shari_ptr]` の重さの2倍以下 (`B[neta_ptr] <= A[shari_ptr] * 2`) であるかを判定します。
        *   **条件を満たす場合:**
            このシャリとネタはペアリングできます。これは、現在のシャリ `A[shari_ptr]` に対して利用可能な最も軽いネタ `B[neta_ptr]` となるため、このペアを採用します。
            *   `count` を `1` 増やします。
            *   両方のポインタを進めます (`shari_ptr += 1`, `neta_ptr += 1`)。これで現在のシャリとネタは使用済みとなります。
        *   **条件を満たさない場合 (`B[neta_ptr] > A[shari_ptr] * 2`):**
            現在のネタ `B[neta_ptr]` は、現在のシャリ `A[shari_ptr]` とは重すぎて組み合わせることができません。また、`B[neta_ptr]` よりも重いネタも当然 `A[shari_ptr]` とは組み合わせられません。
            したがって、現在のシャリ `A[shari_ptr]` はどのネタとも組み合わせられないことが確定します（もしより軽いネタがあったとしても、`neta_ptr` は既にそれらを通過しているため）。
            *   シャリ `A[shari_ptr]` を諦め、次のシャリを探すために `shari_ptr` を進めます (`shari_ptr += 1`)。
            *   ネタ `B[neta_ptr]` は、現在のシャリ `A[shari_ptr]` とは組み合わせられませんでしたが、将来の（より重い）シャリ `A[shari_ptr+1]`, `A[shari_ptr+2]`, ... とは組み合わせられる可能性があるため、`neta_ptr` は進めません。

4.  **結果の出力:** ループが終了したら、`count` の値が最大作成可能数となりますので、これを出力します。

## 計算量

*   **時間計算量:**
    *   シャリ `A` のソート: $O(N \log N)$
    *   ネタ `B` のソート: $O(M \log M)$
    *   2ポインタ法によるペアリング: `shari_ptr` と `neta_ptr` はそれぞれ最大で `N` 回と `M` 回しかインクリメントされないため、$O(N+M)$。
    *   合計: $O(N \log N + M \log M)$
    *   制約 $N, M \leq 2 \times 10^5$ の場合、$2 \times 10^5 \times \log(2 \times 10^5) \approx 2 \times 10^5 \times 18 \approx 3.6 \times 10^6$ 程度の操作回数となり、これは十分に高速です。

*   **空間計算量:**
    *   入力配列 `A`, `B` を格納するのに $O(N+M)$。
    *   Pythonの `list.sort()` は Timsort を使用し、最悪で $O(N)$ の追加空間を必要としますが、通常は $O(\log N)$ または $O(1)$ に近い追加空間で動作します。
    *   合計: $O(N+M)$

## Python 正解模範解答

```python
import sys

def solve():
    N, M = map(int, sys.stdin.readline().split())
    A = list(map(int, sys.stdin.readline().split()))
    B = list(map(int, sys.stdin.readline().split()))

    # シャリとネタをそれぞれ昇順にソートする
    # シャリは小さいものから、ネタも小さいものから見ていくのが貪欲法の基本
    A.sort()
    B.sort()

    shari_ptr = 0  # シャリのリストAを指すポインタ
    neta_ptr = 0   # ネタのリストBを指すポインタ
    count = 0      # 作成できた寿司の数

    # 両方のポインタがリストの範囲内にある間、処理を続ける
    while shari_ptr < N and neta_ptr < M:
        # 現在のシャリA[shari_ptr]と現在のネタB[neta_ptr]を比較
        # ネタの重さがシャリの重さの2倍以下であれば、ペアとして成立
        if B[neta_ptr] <= A[shari_ptr] * 2:
            # ペア成立。現在のシャリとネタを消費する
            count += 1
            shari_ptr += 1  # 次のシャリへ
            neta_ptr += 1   # 次のネタへ
        else:
            # ネタB[neta_ptr]がシャリA[shari_ptr]に対して重すぎる
            # A[shari_ptr]は、B[neta_ptr]よりも重いネタとも組み合わせられない
            # したがって、A[shari_ptr]はこのB[neta_ptr]以降のどのネタとも組み合わせられない
            # A[shari_ptr]を諦めて、次のシャリA[shari_ptr+1]を試す
            shari_ptr += 1
            # B[neta_ptr]は、将来の（より重い）シャリと組み合わせられる可能性があるので、
            # neta_ptrはそのままにしておく
            
    print(count)

if __name__ == '__main__':
    solve()

```

## 実装のポイント

*   **2ポインタ法の理解:** この問題の核心は、ソートされた2つの配列から条件を満たすペアを効率的に見つける2ポインタ法です。一方のポインタを進めるタイミングが重要です。
    *   ペアが成立した場合 (`B[neta_ptr] <= A[shari_ptr] * 2`) は、両方のポインタを進める。
    *   ネタがシャリに対して重すぎた場合 (`B[neta_ptr] > A[shari_ptr] * 2`) は、そのシャリはもう使えないのでシャリ側のポインタのみを進める。ネタはより大きなシャリと組み合わせられる可能性があるので、ネタ側のポインタは進めない。
*   **ソートの重要性:** シャリとネタのリストを両方とも昇順にソートすることが、貪欲法が正しく機能するための前提です。
*   **Pythonのリスト操作の計算量への意識:** `del list[0]` や `list.insert(0, ...)` といったリストの先頭に対する操作は、$O(N)$ の計算量がかかるため、大規模なデータセットでは避けるべきです。代わりに、ポインタやインデックスで管理するか、`collections.deque` のような両端キューを使用することを検討します。
*   **標準入力の高速化:** $N, M \le 2 \times 10^5$ の場合、標準入力の行数や数値の総数が多くなることがあります。`input()` は遅いため、`sys.stdin.readline()` を使用して入力を高速化することが推奨されます。`map(int, sys.stdin.readline().split())` の形式が一般的です。

---
