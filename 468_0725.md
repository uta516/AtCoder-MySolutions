# ABC468 解法レポート

**日付**: 2026年07月25日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC468](https://atcoder.jp/contests/abc468)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - Maximal ValueEditorial

### 問題文

長さ
N
の整数列
A=(A_1,A_2,\ldots,A_N)
が与えられます。
A_i < A_{i+1} > A_{i+2}
を満たす
1
以上
N-2
以下の整数
i
の個数を求めてください。

### 制約

3\le N\le 100
1\le A_i\le 100
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
A_1
A_2
\ldots
A_N

### 出力

答えを出力せよ。

### 入力例 1

6
3 1 4 1 5 2

### 出力例 1

2
i=2,4
の
2
つが条件を満たします。したがって、
2
を出力してください。

### 入力例 2

5
1 1 1 2 1

### 出力例 2

1

### 入力例 3

10
7 3 9 8 10 3 1 5 5 4

### 出力例 3

2

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 48 ms |
| 提出リンク | [#77812907](https://atcoder.jp/contests/abc468/submissions/77812907) |

<details><summary>提出コード</summary>

```python
N = int(input())

A = list(map(int,input().split()))

count = 0
for i in range(N-2):
  if A[i] < A[i+1] and A[i+1] > A[i+2]:
    count += 1

print(count)
```

</details>

---

### 解説

提出コードは問題の要件を完全に満たしており、非常に効率的で適切な解法です。競技プログラミングにおいて、この問題のように制約が小さい（`N <= 100`）場合は、条件に合致するかを愚直にチェックする**全探索**が最もシンプルで、かつ最適解となることが多いです。

## アルゴリズム解説
この問題は、与えられた長さ `N` の整数列 `A` の中で、特定の条件 `A_i < A_{i+1} > A_{i+2}` を満たすインデックス `i` の個数を数えることを求められています。

この条件は、配列の連続する3つの要素 `(A_i, A_{i+1}, A_{i+2})` が「上がってから下がる」というパターン、つまり `A_{i+1}` が局所的な最大値（ピーク）である場合に満たされます。

解法としては、配列 `A` を前から順に走査し、各 `i` に対してこの条件が成立するかどうかを愚直に判定します。

1.  **初期化**: 条件を満たす `i` の個数を数えるためのカウンター `count` を `0` で初期化します。
2.  **ループ**: 問題文では `i` が `1` から `N-2` までの範囲で条件を判定するとあります。Pythonのリストは0-based indexing（インデックスが0から始まる）なので、配列の要素を `A[k]` とすると、対応するインデックス `k` は `0` から `N-3` までの範囲になります。
    *   `A_i` は `A[k]`
    *   `A_{i+1}` は `A[k+1]`
    *   `A_{i+2}` は `A[k+2]`
    そのため、`k` を `0` から `N-3` まで（Pythonの `range(N-2)` に相当）ループさせます。
3.  **条件判定**: ループの各ステップで、現在の `k` について `A[k] < A[k+1]` かつ `A[k+1] > A[k+2]` の両方が真であるかを確認します。
4.  **カウント**: 条件が真であれば、`count` を `1` 増やします。
5.  **出力**: ループが終了したら、最終的な `count` の値を出力します。

## 計算量
-   **時間計算量**: `O(N)`
    *   入力の読み込み: `N` 個の整数を読み込むため `O(N)`。
    *   ループ処理: `N-2` 回ループが実行されます。各ループ内で、配列の要素へのアクセスと2回の比較演算、1回の条件判定、1回の加算という定数時間の操作が行われます。したがって、ループ全体の計算量は `O(N)` です。
    *   合計: 全体として `O(N)` となります。`N` の最大値が100であるため、これは非常に高速に動作します。
-   **空間計算量**: `O(N)`
    *   入力された整数列 `A` を格納するために `N` 個の要素を持つリストが必要となります。
    *   その他の変数 (`N`, `count`, ループ変数 `i` など) は定数空間 `O(1)` です。
    *   合計: 全体として `O(N)` となります。

## Python 模範解答

```python
# N: 数列の長さ
N = int(input())

# A: 整数列
# input().split() でスペース区切りの文字列をリストにし、
# map(int, ...) でそれぞれの文字列を整数に変換、
# list(...) で整数リストとしてAに格納する。
A = list(map(int, input().split()))

# 条件 A_i < A_{i+1} > A_{i+2} を満たすiの個数を数えるカウンター
count = 0

# i は1からN-2 (inclusive) の範囲。
# Pythonの0-based indexでは、配列のインデックス k は 0からN-3 (inclusive) となる。
# k に対応する要素は A[k], A[k+1], A[k+2]。
# k+2 が配列の最大インデックス N-1 を超えないようにする必要があるため、
# k の最大値は N-3。
# したがって、ループは range(N-2) となる。
for k in range(N - 2):
    # 条件 A[k] < A[k+1] かつ A[k+1] > A[k+2] をチェック
    if A[k] < A[k+1] and A[k+1] > A[k+2]:
        # 条件を満たせばカウンターをインクリメント
        count += 1

# 結果を出力
print(count)

```

## 実装のポイント
*   **インデックスの取り扱い**: 問題文のインデックスは1-based (1から始まる) ですが、Pythonのリストは0-based (0から始まる) です。
    *   問題文の `A_i, A_{i+1}, A_{i+2}` は、Pythonコード上では `A[k], A[k+1], A[k+2]` に対応します。
    *   `i` の範囲が `1 \le i \le N-2` なので、`k` の範囲は `0 \le k \le N-3` となります。これは `range(N - 2)` で正確に実現できます。`range(N-2)` は `0, 1, ..., N-3` というインデックスを生成します。
*   **条件の記述**: `A[k] < A[k+1] and A[k+1] > A[k+2]` のように、Pythonの比較演算子と論理演算子 (`and`) を使って条件を正確に記述します。
*   **カウンターの初期化**: ループに入る前に `count = 0` を忘れずに初期化することが重要です。
*   **制約の確認**: `3 \le N \le 100` という制約は非常に小さいため、この単純なループによる全探索が最適なアプローチです。より複雑なアルゴリズムを考える必要はありません。
*   **入力形式**: `input()` と `map(int, input().split())` を使用して、整数 `N` と整数列 `A` を正しく読み込みます。これは競技プログラミングにおけるPythonの典型的な入力方法です。

---

## 問題B: B - Corridor WatchEditorial

### 問題文

整数
M,D
と、
G
,
.
からなる長さ
M
の文字列
S
が与えられます。
M
個のマスが左右に一列に並んでおり、左から順にそれぞれ
1
から
M
までの番号がついています。
いくつかのマスにはガードマンが立っています。具体的には、
S_i=
G
ならばマス
i
にはガードマンが立っており、
S_i=
.
ならばマス
i
には誰も立っていません。
ガードマンが立っているマスからの距離が
D
以下であるマスはガードマンによって監視されます。すなわち、あるマス
i
が存在して
S_i=
G
かつ
|x-i|\le D
を満たすマス
x
はガードマンによって監視されます。
M
個のマスのうち、監視されて
いない
マスの個数を求めてください。

### 制約

0\le D < M \le 100
D,M
は整数
S_i
は
G
と
.
からなる長さ
M
の文字列

### 入力

入力は以下の形式で標準入力から与えられる。
M
D
S

### 出力

答えを出力せよ。

### 入力例 1

7 1
.G...GG

### 出力例 1

1
マス
4
のみが監視されていません。

### 入力例 2

6 5
......

### 出力例 2

6
全てのマスが監視されていません。

### 入力例 3

21 2
....G...GG.....G.....

### 出力例 3

6

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 57 ms |
| 提出リンク | [#77852012](https://atcoder.jp/contests/abc468/submissions/77852012) |

<details><summary>提出コード</summary>

```python
M,D = map(int,input().split())

S = str(input())

g = [1] * M

for i in range(M):
  if S[i] == "G":
    g[i] = 0
    if i+1 >= D:
      for j in range(D+1):
        g[i-j] = 0
       
      if i +1 + D <= M:
        for j in range(D+1):
          g[i+j] = 0
        
      else:
        for j in range(i,M+1):
          
          g[j-1] = 0
         
    else:
      for j in range(i+1):
        g[i-j] = 0
        
      if i + 1+D <= M:
        for j in range(D+1):
          g[i+j] = 0
          
      else:
        for j in range(i,M+1):
          g[j-1] = 0

print(g.count(1))

```

</details>

---

### 解説

## アルゴリズム解説

この問題は、一直線に並んだ `M` 個のマスについて、いくつかのマスにいるガードマンが監視できる範囲を考慮し、最終的にどのマスも監視されていないマスの総数を求めるものです。

私たちが求めるのは「監視されていないマスの数」です。この種の「範囲の活性化」や「状態の伝播」を扱う問題では、通常以下のようなアプローチが有効です。

1.  **状態の初期化**: まず、`M`個のマスすべてが「監視されていない」と仮定して、その状態を記録する配列（リスト）を作成します。例えば、`watched = [False] * M` のようにブーリアン型のリストを用意します。
2.  **ガードマンの位置を走査**: 与えられた文字列 `S` を先頭から順に走査し、ガードマン (`'G'`) がいるマスを見つけます。マスのインデックスは `0` から `M-1` とします。
3.  **監視範囲のマーク**: ガードマンがマス `i` にいる場合、そのガードマンはマス `x` で `|x - i| <= D` を満たす全てのマスを監視します。これは、閉区間 `[i - D, i + D]` に該当します。
    *   この区間内の各マス `j` について、`watched[j] = True` と更新します。
    *   重要なのは、マスのインデックスは `0` から `M-1` の範囲に限定されるため、実際にマークする範囲は `[max(0, i - D), min(M - 1, i + D)]` となります。配列の範囲外へのアクセスを防ぐために、この境界条件を適切に処理する必要があります。
4.  **結果の集計**: 全てのガードマンについて監視範囲のマークが完了した後、`watched` リストの中で `False` の要素（つまり、どのガードマンにも監視されていないマス）の数を数え、それが答えとなります。

このアルゴリズムは、各ガードマンの監視範囲を正確に反映し、重複する監視も問題なく処理します（すでに `True` になっているマスを再度 `True` にしても結果に影響はありません）。

## 計算量

*   **時間計算量**:
    *   `watched` リストの初期化: `O(M)`
    *   文字列 `S` の走査: `M` 回のループ。
    *   各ガードマンが見つかった際、監視範囲をマークする内部ループ: 最大で `2D + 1` 回の操作。
    *   最悪の場合、全てのマスにガードマンがいるとすると、`M` 回の外部ループと `M * (2D + 1)` 回の内部ループ操作が発生します。
    *   したがって、監視範囲のマークにかかる時間は `O(M * D)`。
    *   最後に `watched` リストの `False` を数える操作: `O(M)`。
    *   全体として、時間計算量は **O(M * D)** となります。
    *   制約 `M <= 100`, `D < M` (最大 `D=99`) を考えると、`100 * 99` は約 `10^4` 程度の操作数であり、十分に高速です。
*   **空間計算量**:
    *   `watched` リストを保持するために `M` 要素のメモリが必要です。
    *   したがって、空間計算量は **O(M)** となります。
    *   制約 `M <= 100` を考えると、非常に少ないメモリ使用量です。

## Python 模範解答

```python
M, D = map(int, input().split())
S = input() # input() はデフォルトで文字列を返すため、str() は不要

# 各マスが監視されているかを示すブーリアンリストを初期化
# watched[i] = True ならマス i は監視されている、False ならされていない
# 最初は全てのマスが監視されていないと仮定する
watched = [False] * M

# 文字列 S を走査し、ガードマンの位置を探す
for i in range(M):
    if S[i] == 'G':
        # マス i にガードマンがいる場合、
        # そのガードマンは区間 [i - D, i + D] のマスを監視する。
        # ただし、マスのインデックスは 0 から M-1 の範囲でなければならない。

        # 監視範囲の開始インデックスを計算
        # max(0, i - D) により、インデックスが 0 未満にならないようにする
        start_idx = max(0, i - D)

        # 監視範囲の終了インデックスを計算
        # min(M - 1, i + D) により、インデックスが M-1 を超えないようにする
        end_idx = min(M - 1, i + D)

        # 計算された監視範囲内の全てのマスを「監視されている」とマークする
        # Pythonのrangeは終了値を含まないので、end_idx + 1 を指定する
        for j in range(start_idx, end_idx + 1):
            watched[j] = True

# 全てのガードマンの監視範囲を処理した後、
# watched リストの中で False の要素（監視されていないマス）の数を数える
unwatched_count = watched.count(False)

# 結果を出力
print(unwatched_count)

```

## 実装のポイント

*   **0-indexedの利用**: 問題文ではマスが1からMまでとされていますが、Pythonの文字列やリストは0-indexed（0からM-1）で扱うのが自然で、コードが簡潔になります。今回の模範解答では、文字列 `S` の `S[i]` と対応するマスをインデックス `i` のマスとして扱い、`0` から `M-1` の範囲で処理を統一しています。
*   **境界条件の正確な処理**: ガードマン `i` の監視範囲は `[i - D, i + D]` ですが、この範囲がマスの有効なインデックス `[0, M-1]` を超える可能性があります。
    *   左端については `max(0, i - D)` を使って、インデックスが負になるのを防ぎます。
    *   右端については `min(M - 1, i + D)` を使って、インデックスが `M-1` を超えるのを防ぎます。
    *   これにより、`IndexError` を回避し、正確な監視範囲をマークできます。
*   **ブーリアンリストの使用**: 各マスが監視されているかどうかの状態を `True`/`False` で管理するブーリアンリスト `watched` を使用することで、コードの意図が明確になり、可読性が向上します。
*   **`list.count()`の活用**: 最後に監視されていないマスの数を数える際に、`watched.count(False)` を使用することで、ループを書かずに効率的に数を取得できます。

---
