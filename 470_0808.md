# ABC470 解法レポート

**日付**: 2026年08月08日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC470](https://atcoder.jp/contests/abc470)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題B: B - MonocolorEditorial

### 問題文

N
個のボールがあります。各ボールは色
1
から色
N
までの
N
色のいずれかで塗られており、
i
個目
(1\le i\le N)
のボールの色は
C_i
です。
あなたは
1
回の操作で好きなボール
1
つを好きな
N
色のいずれかに変更することができます。
全てのボールが同じ色になるようにするためには、最小で何回の操作が必要か求めてください。

### 制約

1\le N\le 100
1\le C_i\le N
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
C_1
C_2
\ldots
C_N

### 出力

答えを出力せよ。

### 入力例 1

4
3 1 2 1

### 出力例 1

2
1
個目のボールの色を
1
に、
3
個目のボールの色を
1
にすることで全てのボールの色を同じ色にすることができます。
2
個未満のボールの色を変えることで全てのボールの色を同じ色にすることはできないので、
2
を出力してください。

### 入力例 2

5
3 3 3 3 3

### 出力例 2

0
最初から全てのボールの色が同じ場合もあります。

### 入力例 3

9
4 2 3 3 4 1 2 7 1

### 出力例 3

7

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 62 ms |
| 提出リンク | [#78205905](https://atcoder.jp/contests/abc470/submissions/78205905) |

<details><summary>提出コード</summary>

```python
from collections import Counter

N = int(input())
C = map(int,input().split())

counter = Counter(C)

if len(counter) == 1:
  print(0)
else:
  print(N - counter.most_common()[0][1])

```

</details>

---

### 解説

## アルゴリズム解説

この問題は、N個のボールを全て同じ色にするために必要な最小操作回数を求めるものです。1回の操作で任意のボール1つの色を好きな色に変更できます。

**何を求めているか**: 全てのボールを同じ色にするための最小操作回数。

**どの手法で解くか**:
最終的に全てのボールを同じ色にするには、N色のうちいずれか1色を「目標の色」として選び、それ以外の色のボールを全て目標の色に変更する必要があります。
操作回数を最小にするためには、すでに最も多く存在する色を目標の色として選ぶのが最適です。なぜなら、その色以外のボールだけを操作すればよく、変更する必要があるボールの数が最も少なくなるからです。

具体的なアルゴリズムは以下の通りです。

1.  まず、与えられたN個のボールの色 `C_i` のリストを読み込みます。
2.  各色が何回出現するかを数えます。Pythonの `collections.Counter` を使うと、この作業を非常に効率的に行えます。`Counter` オブジェクトは、要素をキーとし、その出現回数を値とする辞書のような振る舞いをします。
3.  `Counter` オブジェクトが保持するキー（色）の数が1つだけの場合、これは全てのボールが最初から同じ色であることを意味します。この場合、操作は不要なので、答えは0です。
4.  キーの数が複数ある場合（つまり、複数の色が存在する場合）、最も多く出現する色を目標の色として選びます。`Counter.most_common()` メソッドは、出現回数が多い順に要素とその回数のペアをリストで返します。このリストの最初の要素が、最も出現回数の多い色とその回数です。
5.  N個のボールの総数から、最も多く存在する色の出現回数を引いたものが、変更する必要のあるボールの数、すなわち最小操作回数となります。

## 計算量

-   **時間計算量**: O(N log N)
    -   入力の読み込みと `map` オブジェクトの生成: O(N)
    -   `collections.Counter(C)` による各色の出現回数のカウント: O(N) (各ボールの色を一度だけ処理するため)
    -   `counter.most_common()` による出現回数が多い順のソート: O(D log D)。ここでDは異なる色の数で、D <= N です。最悪の場合、全てのボールが異なる色なのでD=Nとなり、O(N log N)となります。
    -   それ以外の処理（`len()` やリストのインデックスアクセス、引き算など）はO(1)です。
    -   したがって、全体の時間計算量は **O(N log N)** となります。
    -   なお、`counter.most_common()[0][1]` の代わりに `max(counter.values())` を使用すれば、最大値を探す処理はO(D)となり、全体の時間計算量をO(N)にすることができます。Nの制約が100と小さいため、どちらの方法でも十分高速です。

-   **空間計算量**: O(N)
    -   `input().split()` で生成される文字列のリスト、および `collections.Counter` オブジェクトが異なる色の数（最大N個）とその出現回数を格納するために、O(N)の空間が必要となります。

## Python 模範解答

```python
from collections import Counter

# N: ボールの総数を読み込む
N = int(input())

# C: N個のボールの色リストを読み込む
# input().split() でスペース区切りの文字列をリストにし、map(int, ...) で整数に変換
C = map(int, input().split())

# collections.Counter を使用して、各色の出現回数をカウント
# 例: C = [3, 1, 2, 1] -> counter = {3: 1, 1: 2, 2: 1}
counter = Counter(C)

# もし、カウンターの長さが1であれば、全てのボールが既に同じ色
# 例: C = [3, 3, 3, 3, 3] -> counter = {3: 5} -> len(counter) == 1
if len(counter) == 1:
  # 操作は不要なので0を出力
  print(0)
else:
  # 複数の色が存在する場合、最も多く存在する色を目標の色とする
  # most_common() は出現回数が多い順に (要素, 回数) のリストを返す
  # most_common()[0] は最も出現回数の多い (色, 回数) のペア
  # most_common()[0][1] は最も出現回数の多い色の出現回数
  max_count = counter.most_common()[0][1]
  
  # 全体のボール数Nから、最も多く存在する色のボールの数を引く
  # これが、色を変更する必要があるボールの数、すなわち最小操作回数
  print(N - max_count)

```

## 実装のポイント

*   **`collections.Counter` の活用**: 各要素の出現回数を数えるのは競技プログラミングで頻出するタスクです。Pythonの `collections.Counter` は、このタスクを非常に簡潔かつ効率的に記述できる強力なツールです。手動で辞書を使ってカウントするよりもバグが少なく、読みやすいコードになります。
*   **「既に同じ色」の場合の処理**: 問題によっては、初期状態で既に条件を満たしているケースを考慮し忘れることがあります。今回の問題では、全てのボールが最初から同じ色である場合（`len(counter) == 1`）、操作回数は0となるため、このケースを明示的に処理することが重要です。
*   **最適解の導出ロジック**: 「最も多く存在する色を維持し、それ以外の色をその色に変更する」というシンプルながら最適な戦略を理解することが重要です。これにより、Nから最大出現回数を引くだけで答えが求まります。

---

## 問題C: C - Inc, Dec, XorEditorial

### 問題文

長さ
N
の整数列
A=(A_1,A_2,\ldots,A_N)
があります。はじめ、
A
の要素は全て
0
です。
Q
個のクエリが与えられるので、順に処理してください。クエリは
2
種類あり、以下のいずれかの形式で与えられます。
1 x
：
A_x
の値を
1
増やす。
2
：
i=1,2,\ldots,N
に対し、
A_i \geq 1
ならば
A_i
の値を
1
減らす。
各クエリを処理した直後の
A_1,A_2,\ldots,A_N
のビット単位
\mathrm{XOR}
を求めてください。
ビット単位
\mathrm{XOR}
演算とは
非負整数
A, B
のビット単位
\mathrm{XOR}
、
A \oplus B
は、以下のように定義されます。
A \oplus B
を二進表記した際の
2^k
(
k \geq 0
) の位の数は、
A, B
を二進表記した際の
2^k
の位の数のうち一方のみが
1
であれば
1
、そうでなければ
0
である。
例えば、
3 \oplus 5 = 6
となります (二進表記すると:
011 \oplus 101 = 110
)。
一般に
k
個の非負整数
p_1, p_2, p_3, \dots, p_k
のビット単位
\mathrm{XOR}
は
(\dots ((p_1 \oplus p_2) \oplus p_3) \oplus \dots \oplus p_k)
と定義され、これは
p_1, p_2, p_3, \dots, p_k
の順番によらないことが証明できます。

### 制約

1\le N\le 5\times 10^5
1\le Q\le 5\times 10^5
1\le x\le N
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
Q
\text{query}_1
\text{query}_2
\vdots
\text{query}_Q
各クエリは以下の
2
種類のいずれかの形式で与えられる。
1
x
2

### 出力

Q
行出力せよ。
i
行目
(1\le i\le Q)
には、
i
番目のクエリを処理した直後の
A
に対する
A_1,A_2,\ldots,A_N
のビット単位
\mathrm{XOR}
を出力せよ。

### 入力例 1

2 5
1 2
1 2
1 1
2
2

### 出力例 1

1
2
3
1
0
1
番目のクエリを処理した後
A=(0,1)
となります。
0,1
のビット単位
\mathrm{XOR}
は
1
なので、
1
行目には
1
を出力してください。
2
番目のクエリを処理した後
A=(0,2)
となります。
0,2
のビット単位
\mathrm{XOR}
は
2
なので、
2
行目には
2
を出力してください。
3
番目のクエリを処理した後
A=(1,2)
となります。
1,2
のビット単位
\mathrm{XOR}
は
3
なので、
3
行目には
3
を出力してください。
4
番目のクエリを処理した後
A=(0,1)
となります。
0,1
のビット単位
\mathrm{XOR}
は
1
なので、
4
行目には
1
を出力してください。
5
番目のクエリを処理した後
A=(0,0)
となります。
0,0
のビット単位
\mathrm{XOR}
は
0
なので、
5
行目には
0
を出力してください。

### 入力例 2

3 8
1 2
1 3
1 1
1 2
1 1
2
1 3
1 1

### 出力例 2

1
0
1
2
1
0
1
2

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **❌ TLE** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | None ms |
| 提出リンク | [#78231767](https://atcoder.jp/contests/abc470/submissions/78231767) |

<details><summary>提出コード</summary>

```python
import numpy as np

N , Q = map(int,input().split())
A = [0] * N

def judge(A):
  a = np.array(A, dtype=np.int64)
  return np.bitwise_xor.reduce(a)

S = [input().split() for _ in range(Q)]

s = 0
x = 0
for i in range(Q):
  s = int(S[i][0])
  if s == 1:
    x = int(S[i][1])
    A[x - 1] += 1
  else:
    for j in range(N):
      if A[j] >= 1:
        A[j] -= 1
  print(judge(A))
  
```

</details>

---

### 解説

## TLE の原因分析

提出されたPythonコードがTLEとなる原因は、主に以下の2点における計算量の問題です。

1.  **各クエリでのXOR和の再計算 (`judge` 関数):**
    `judge` 関数内で、毎回 `A` リストをNumPy配列に変換し (`np.array(A, dtype=np.int64)`)、その後 `np.bitwise_xor.reduce(a)` を用いてXOR和を計算しています。
    *   リストからNumPy配列への変換は `O(N)` の時間がかかります。
    *   `np.bitwise_xor.reduce` も配列の全要素を走査するため `O(N)` の時間がかかります。
    したがって、各クエリごとに `O(N)` のコストが発生します。

2.  **クエリタイプ2での配列の全走査:**
    クエリタイプ2 (`s == 2`) では、`for j in range(N):` ループを用いて配列 `A` の全要素を走査し、`A[j] >= 1` であれば `A[j]` をデクリメントしています。
    これは常に `O(N)` の時間がかかります。

これらの処理により、1つのクエリあたりの最悪計算量は `O(N)` となります。
クエリが `Q` 個あるため、全体の計算量は `O(Q * N)` となります。
問題の制約は `N, Q

---
