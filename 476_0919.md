# ABC476 解法レポート

**日付**: 2026年09月20日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC476](https://atcoder.jp/contests/abc476)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - AppenderEditorial

### 問題文

英小文字からなる文字列
S
が与えられます。
以下のようにして決まる文字列
T
を出力してください。
S
の末尾の文字が
e
である場合、
T
は
S
の末尾に
r
を付け加えた文字列である。
S
の末尾の文字が
e
でない場合、
T
は
S
の末尾に
er
を付け加えた文字列である。

### 制約

S
は英小文字からなる文字列
S
の長さは
1
以上
10
以下

### 入力

入力は以下の形式で標準入力から与えられる。
S

### 出力

答えを出力せよ。

### 入力例 1

live

### 出力例 1

liver
live
の末尾の文字は
e
なので、
live
の末尾に
r
を付け加えた文字列
liver
が出力するべき文字列
T
です。

### 入力例 2

femur

### 出力例 2

femurer
femur
の末尾の文字は
e
ではないので、
femur
の末尾に
er
を付け加えた文字列
femurer
が出力するべき文字列
T
です。

### 入力例 3

chimpanzee

### 出力例 3

chimpanzeer
chimpanzee
の末尾の文字は
e
なので、
chimpanzee
の末尾に
r
を付け加えた文字列
chimpanzeer
が出力するべき文字列
T
です。

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 49 ms |
| 提出リンク | [#79346203](https://atcoder.jp/contests/abc476/submissions/79346203) |

<details><summary>提出コード</summary>

```python
S = str(input())

n = len(S)

if S[n-1] == "e":
  T = S + "r"
else:
  T = S + "er"
  
print(T)
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられた文字列 `S` の末尾の文字を調べて、その結果に基づいて新しい文字列 `T` を作成し、出力するというものです。

解法は以下の手順で行われます。

1.  **入力**: 標準入力から文字列 `S` を読み込みます。
2.  **末尾の文字の判定**: 文字列 `S` の最後の文字が `'e'` であるかどうかをチェックします。Pythonでは、文字列の末尾の文字は `S[-1]` で簡単にアクセスできます。
3.  **条件に応じた文字列の生成**:
    *   もし `S` の末尾の文字が `'e'` であれば、`S` の末尾に `'r'` を結合して新しい文字列 `T` を作成します。
    *   もし `S` の末尾の文字が `'e'` でなければ、`S` の末尾に `'er'` を結合して新しい文字列 `T` を作成します。
4.  **出力**: 作成した文字列 `T` を標準出力に出力します。

これは、基本的な条件分岐（`if-else`文）と文字列操作を用いた非常にシンプルな問題です。

## 計算量

*   **時間計算量**: `O(L)`
    *   `S` の読み込み: `O(L)`
    *   末尾の文字へのアクセス: `O(1)` (インデックスアクセス)
    *   文字列の比較: `O(1)`
    *   文字列の結合: `S` の長さが `L` で、結合する文字列の長さが定数（1または2）であるため、新しい文字列 `T` の生成にかかる時間は `O(L+1)` または `O(L+2)` となり、これは `O(L)` です。
    *   `T` の出力: `O(L)`
    総合すると、文字列の長さ `L` に比例する時間計算量となります。問題の制約で `L` は最大10なので、非常に高速に処理されます。

*   **空間計算量**: `O(L)`
    *   入力文字列 `S` を格納するために `O(L)` の空間が必要です。
    *   出力文字列 `T` は `S` に数文字を追加したものなので、これも `O(L)` の空間が必要です。
    総合すると、文字列の長さ `L` に比例する空間計算量となります。

## Python 模範解答

```python
# 標準入力から文字列 S を受け取る
S = input()

# S の末尾の文字を判定する
# Pythonでは、負のインデックスを使うと文字列の末尾から文字にアクセスできます。
# S[-1] は文字列 S の最後の文字を指します。
if S[-1] == 'e':
  # 末尾の文字が 'e' の場合、S に 'r' を追加する
  T = S + 'r'
else:
  # 末尾の文字が 'e' でない場合、S に 'er' を追加する
  T = S + 'er'
  
# 結果の文字列 T を出力する
print(T)
```

## 実装のポイント

*   **末尾の文字へのアクセス**: Pythonでは文字列の末尾の文字にアクセスする際に `S[len(S)-1]` と書くこともできますが、より簡潔でPythonicな `S[-1]` を使うのが一般的です。これはコードの可読性を高めます。
*   **文字列の結合**: Pythonで文字列を結合するには `+` 演算子を使用します。この操作は新しい文字列オブジェクトを生成しますが、この問題のように文字列の長さが非常に短く、結合回数が少ない場合は性能上の問題は全くありません。
*   **制約の確認**: 問題文の制約で `S` の長さは1以上と保証されています。これにより、`S` が空文字列である可能性を考慮する必要がなく、`S[-1]` のようなインデックスアクセスが常に安全に行えることが保証されます。
*   **問題文の正確な理解**: 「末尾が 'e' の場合」と「末尾が 'e' でない場合」の2つの条件分岐を正確に実装することが重要です。条件を見誤ると、期待される出力と異なる結果になります。

---

## 問題B: B - Wild CardEditorial

### 問題文

S
は英小文字からなる長さ
N
の文字列です。
T
は英小文字および
*
からなる長さ
N
の文字列です。
S
が
T
にマッチするとは、
T
に含まれる
*
をそれぞれ好きな英小文字で置き換えることで
T
を
S
に一致させられることをいいます。
S
が
T
にマッチするかどうかを判定してください。

### 制約

1 \leq N \leq 100
S
は英小文字からなる長さ
N
の文字列
T
は英小文字および
*
からなる長さ
N
の文字列

### 入力

入力は以下の形式で標準入力から与えられる。
N
S
T

### 出力

S
が
T
にマッチするならば
Yes
を、マッチしないならば
No
を
1
行で出力せよ。

### 入力例 1

8
chokudai
**o*u*ai

### 出力例 1

Yes
T
の
1,2,4,6
文字目の
*
をそれぞれ
c
,
h
,
k
,
d
に置き換えることで
T
を
S
に一致させることができます。

### 入力例 2

5
snuke
snake

### 出力例 2

No

### 入力例 3

5
yiwiy
*****

### 出力例 3

Yes

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 49 ms |
| 提出リンク | [#79352108](https://atcoder.jp/contests/abc476/submissions/79352108) |

<details><summary>提出コード</summary>

```python
N = int(input())
S = str(input())
T = str(input())

result = 0
for i in range(N):
  if T[i] != "*":
    if S[i] != T[i]:
      result = 1
      

if result == 0:
  print("Yes")
else:
  print("No")
    
```

</details>

---

### 解説

## アルゴリズム解説

この問題では、長さ `N` の文字列 `S` と `T` が与えられ、`S` が `T` にマッチするかどうかを判定します。`T` に含まれる `*`（ワイルドカード）は任意の英小文字に置き換えることができます。

`S` が `T` にマッチするかどうかは、以下の条件に基づいて各文字を比較することで判定できます。

1.  **`T[i]` が `*` の場合**:
    `*` は任意の英小文字に置き換え可能であるため、`S[i]` が何であっても、`T[i]` を `S[i]` に一致させることができます。この場合、常にマッチするとみなします。

2.  **`T[i]` が `*` ではない（つまり英小文字）の場合**:
    `T[i]` は固定された文字であるため、`S[i]` と `T[i]` が完全に一致している必要があります。もし `S[i]` と `T[i]` が異なっていれば、この位置で文字列を一致させることができません。この場合、`S` は `T` にマッチしません。

これらのルールに基づいて、文字列の先頭から末尾まで1文字ずつ比較していきます。
- もし1箇所でも「`T[i]` が `*` ではない」かつ「`S[i]` と `T[i]` が異なる」という条件が満たされた場合、その時点で `S` は `T` にマッチしないと確定します。
- 全ての文字を比較し終えても上記の不一致条件が一度も満たされなかった場合、`S` は `T` にマッチすると判定できます。

## 計算量

*   **時間計算量**: O(N)
    文字列の長さ `N` に比例して、各文字を1回ずつ比較します。`N` のループが1回実行されるため、時間計算量は `O(N)` となります。`N` は最大100なので、非常に高速に処理できます。

*   **空間計算量**: O(N)
    入力として与えられる文字列 `S` と `T` をそれぞれ長さ `N` の変数として格納する必要があるため、空間計算量は `O(N)` となります。

## Python 模範解答

```python
N = int(input())
S = input() # 文字列Sを読み込む
T = input() # 文字列Tを読み込む

# マッチするかどうかのフラグ。初期値はTrue（マッチすると仮定）
is_match = True

# 文字列の各インデックスiについて比較を行う
for i in range(N):
    # Tのi番目の文字がワイルドカード('*')でない場合のみチェックが必要
    if T[i] != '*':
        # Tのi番目の文字が'*'でなく、かつSのi番目の文字と異なる場合
        # これはマッチしない条件に該当する
        if S[i] != T[i]:
            is_match = False # マッチしないことを示すフラグを立てる
            break            # 一度不一致が見つかったら、それ以上比較する必要はないためループを抜ける

# 全ての文字を比較し終えた後、is_matchフラグの値に応じて結果を出力
if is_match:
    print("Yes")
else:
    print("No")

```

## 実装のポイント

*   **1文字ずつの比較**: `S` と `T` の各文字を対応するインデックスで1つずつ比較することが重要です。
*   **ワイルドカードの扱い**: `T[i]` が `*` の場合は、`S[i]` の内容に関わらず常にマッチします。このケースでは、何も処理せず次の文字に進めます。
*   **不一致の判定**: `T[i]` が `*` でない場合、`S[i]` と `T[i]` が完全に一致している必要があります。もし一致しない場合は、そこで全体としてマッチしないことが確定します。
*   **早期終了 (break)**: 一度でもマッチしない箇所が見つかったら、それ以降の文字を比較する必要はありません。`break`文を使ってループを中断することで、わずかながら処理を効率化できます。
*   **フラグ変数の利用**: `is_match` のようなブーリアン型のフラグ変数を用いることで、ループ内で見つかった条件（この場合は不一致）を記憶し、ループ終了後にその結果に基づいて最終的な判断を下すことができます。
*   **Nの制約**: `N` が最大100と非常に小さいため、上記の単純なループによる全走査でも時間制限に引っかかる心配はありません。最適なアルゴリズムをシンプルに実装することが重要です。

---

## 問題C: C - Third Largest NumberEditorial

### 問題文

3
以上の整数
N
と長さ
N
の正整数列
A=(A_1,A_2,\ldots,A_N)
が与えられます。
k=3,4,\ldots,N
それぞれに対し以下の問題を解いてください：
A_1,A_2,\ldots,A_k
を降順に並べたとき、前から
3
番目にくる値を求めてください。

### 制約

3\le N\le 5\times 10^5
1\le A_i\le 10^9
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
A_1
A_2
\ldots
A_N

### 出力

N-2
行出力せよ。
i
行目
(1\le i\le N-2)
には
k=i+2
とした時の答えを出力せよ。

### 入力例 1

5
1 2 1 2 3

### 出力例 1

1
1
2
k=3
のとき：
A_1,A_2,A_3
を降順に並べると
2,1,1
となります。前から
3
番目に来る値は
1
なので、
1
行目には
1
を出力してください。
k=4
のとき：
A_1,A_2,A_3,A_4
を降順に並べると
2,2,1,1
となります。前から
3
番目に来る値は
1
なので、
2
行目には
1
を出力してください。
k=5
のとき：
A_1,A_2,A_3,A_4,A_5
を降順に並べると
3,2,2,1,1
となります。前から
3
番目に来る値は
2
なので、
3
行目には
2
を出力してください。

### 入力例 2

10
1 1 1 3 2 5 4 3 6 5

### 出力例 2

1
1
1
2
3
3
4
5

### 入力例 3

10
11 9 1 3 17 19 10 19 17 3

### 出力例 3

1
3
9
11
11
17
17
17

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 412 ms |
| 提出リンク | [#79370227](https://atcoder.jp/contests/abc476/submissions/79370227) |

<details><summary>提出コード</summary>

```python
N = int(input())
A = list(map(int,input().split()))

B = A[0:3]
B.sort(reverse=True)
third = B[2]
b2=B[1]
b1=B[0] 
print(third)

for i in range(3,N):
  if A[i] >= third and A[i] <= b2:
    third = A[i]
    print(third)
  elif A[i] > third and A[i] > b2:
    if A[i] > b1:
      third = b2
      b2 = b1
      b1 = A[i]
    else:
      third = b2
      b2 = A[i]
    print(third)
  else:
    print(third)
  
    
      
  
  
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられた数列 $A$ の先頭から $k$ 個の要素 $(A_1, \ldots, A_k)$ について、その中で3番目に大きい値を $k=3, 4, \ldots, N$ のそれぞれで求めるものです。

$N$ の最大値が $5 \times 10^5$ と大きいため、各 $k$ のステップで $A_1, \ldots, A_k$ のサブリストをソートする ($O(k \log k)$) と、全体で $O(N^2 \log N)$ となり時間制限に間に合いません。

そこで、**各ステップで効率的にトップ3の値を更新していく**というアプローチを取ります。
具体的には、以下の3つの変数を常に管理します。
1.  `first_max`: 現在の範囲 ($A_1, \ldots, A_k$) での最大値
2.  `second_max`: 現在の範囲での2番目に大きい値
3.  `third_max`: 現在の範囲での3番目に大きい値

これらの変数は常に `first_max >= second_max >= third_max` の関係を保つようにします。

**アルゴリズムの手順:**

1.  **初期化 ($k=3$ の場合):**
    最初の3つの要素 ($A_0, A_1, A_2$) を取り出し、降順にソートします。
    - ソート結果の1番目の値を `first_max` に。
    - ソート結果の2番目の値を `second_max` に。
    - ソート結果の3番目の値を `third_max` に。
    この `third_max` が $k=3$ のときの答えなので、出力します。

2.  **逐次更新 ($k=4, \ldots, N$ の場合):**
    $i=3$ から $N-1$ までループを回し、各ステップで新しい要素 $A_i$ を現在の範囲に追加します。この $A_i$ が `current_val` となります。
    `current_val` と現在の `first_max`, `second_max`, `third_max` を比較し、適切に更新します。

    -   **ケース1: `current_val > first_max`**
        新しい値がこれまでの最大値よりも大きい場合。
        `current_val` が新しい `first_max` になります。
        これまでの `first_max` は新しい `second_max` になります。
        これまでの `second_max` は新しい `third_max` になります。
        (`third_max = second_max`, `second_max = first_max`, `first_max = current_val`)

    -   **ケース2: `first_max >= current_val > second_max`**
        新しい値がこれまでの最大値以下ですが、2番目に大きい値よりも大きい場合。
        `current_val` が新しい `second_max` になります。
        これまでの `second_max` は新しい `third_max` になります。
        `first_max` は変わりません。
        (`third_max = second_max`, `second_max = current_val`)

    -   **ケース3: `second_max >= current_val > third_max`**
        新しい値がこれまでの2番目に大きい値以下ですが、3番目に大きい値よりも大きい場合。
        `current_val` が新しい `third_max` になります。
        `first_max`, `second_max` は変わりません。
        (`third_max = current_val`)

    -   **ケース4: `current_val <= third_max`**
        新しい値がこれまでの3番目に大きい値以下の場合。
        トップ3には影響しないため、`first_max`, `second_max`, `third_max` は何も変わりません。

    各ステップで更新が終わった後、その時点での `third_max` を出力します。

このアプローチにより、各ステップでの更新が定数時間 $O(1)$ で行われるため、全体として非常に効率的です。

## 計算量

-   **時間計算量: O(N)**
    -   初期化 ($k=3$): 最初の3要素のソートは定数時間 $O(1)$。
    -   逐次更新 ($k=4, \ldots, N$): $N-3$ 回のループで、各ループ内は比較と代入のみで定数時間 $O(1)$。
    -   合計で $O(1) + O(N-3) = O(N)$ となります。

-   **空間計算量: O(N)**
    -   入力配列 $A$ を保持するために $O(N)$ の空間が必要です。
    -   `first_max`, `second_max`, `third_max` などの変数は定数個なので $O(1)$ です。
    -   合計で $O(N)$ となります。

## Python 模範解答

```python
N = int(input())
A = list(map(int, input().split()))

# 最初の3つの要素で最大値、2番目の最大値、3番目の最大値を初期化する
# k=3 の場合に対応
initial_three = sorted(A[0:3], reverse=True) # 降順にソート
first_max = initial_three[0]  # 現在の範囲での最大値
second_max = initial_three[1] # 現在の範囲での2番目に大きい値
third_max = initial_three[2]  # 現在の範囲での3番目に大きい値

# k=3 の結果を出力
print(third_max)

# k=4 から N までの場合を順に処理する
# A[3] から A[N-1] までの要素を一つずつ追加していく
for i in range(3, N):
    current_val = A[i] # 新しく追加される要素

    # 新しい値が現在の最大値、2番目の最大値、3番目の最大値のいずれかに影響するかを判定
    # first_max >= second_max >= third_max の関係を維持しながら更新する
    if current_val > first_max:
        # ケース1: 新しい値が現在の最大値よりも大きい場合
        # 順位が一つずつ繰り下がる
        third_max = second_max
        second_max = first_max
        first_max = current_val
    elif current_val > second_max:
        # ケース2: 新しい値が最大値よりは小さいか等しいが、2番目の最大値よりも大きい場合
        # 2番目の最大値と3番目の最大値が更新される
        third_max = second_max
        second_max = current_val
    elif current_val > third_max:
        # ケース3: 新しい値が2番目の最大値よりは小さいか等しいが、3番目の最大値よりも大きい場合
        # 3番目の最大値のみが更新される
        third_max = current_val
    # else (current_val <= third_max):
        # ケース4: 新しい値が現在の3番目の最大値以下の場合
        # トップ3には影響しないので、first_max, second_max, third_max は変わらない

    # その時点での3番目の最大値を出力
    print(third_max)
```

## 実装のポイント

*   **3つの変数の役割**: `first_max`, `second_max`, `third_max` の3つの変数を使い、常に現在の範囲における最大値、2番目に大きい値、3番目に大きい値を保持するようにします。これらの変数は常に `first_max >= second_max >= third_max` の関係を満たすように更新します。
*   **初期化の正確性**: 最初の3つの要素 (`A[0]`, `A[1]`, `A[2]`) を使ってこれら3変数を正しく初期化することが重要です。このために、最初の3要素をソートするのが最も確実です。
*   **場合分けの網羅と順序**: 新しい要素 `current_val` が来たときに、どの変数を更新すべきかを正確に判定するための `if-elif` の条件分岐が重要です。条件は「最も大きい値かどうか」「2番目に大きい値かどうか」「3番目に大きい値かどうか」の順でチェックすることで、正しく更新が伝播します。
*   **重複値の扱い**: 問題文では「降順に並べたとき」とあり、例えば `(5, 4, 4, 3)` の3番目は `4` です。上記のコードでは比較に `>=` や `>` を適切に使用しているため、同じ値が複数ある場合も正しく処理されます。例えば、`first_max = 5, second_max = 4, third_max = 3` のときに `current_val = 4` が来ると、「ケース2」に該当し、`third_max` が `4` に更新され、`second_max` も `4` になります。
*   **出力のタイミング**: $k=3$ の場合から $N$ の場合まで、各ステップで計算結果を逐次出力する必要があります。ループの前に1回、ループ内で $N-3$ 回出力することで、合計 $N-2$ 行の出力形式に合致します。

---

## 問題D: D - AutomatEditorial

### 問題文

AtCoder 王国では、
1
ドル紙幣と
K
ドル紙幣の
2
種類の紙幣が流通しています。
AtCoder 王国にある J 社の全自動食堂では、デザートやドリンクが商品として売られています。
J 社の全自動食堂には、
N
個のデザートを売るデザート販売機と、
M
個のドリンクを売るドリンク販売機があります。
デザートには
1
から
N
までの、ドリンクには
1
から
M
までの番号が付けられています。
デザート
i
の価格は
A_i
ドル、ドリンク
j
の価格は
B_j
ドルです。
支払いに使う紙幣として、デザート販売機は
1
ドル紙幣と
K
ドル紙幣の両方を受け入れますが、ドリンク販売機は
K
ドル紙幣しか受け入れません。
どちらの販売機も、お釣りはすべて
1
ドル紙幣のみを使って返します。
同じ商品を
2
個以上買うことはできません。
高橋君は
1
ドル紙幣
X
枚と
K
ドル紙幣
Y
枚を持って J 社の全自動食堂にやってきました。
高橋君が手持ちの紙幣で買える商品の組合せにおいて、購入できる商品数の最大値を求めてください。

### 制約

1 \leq N \leq 2 \times 10^5
1 \leq M \leq 2 \times 10^5
2 \leq K \leq 10^9
0 \leq X \leq 10^{15}
0 \leq Y \leq 10^9
1 \leq A_i \leq 10^9
(
1 \leq i \leq N
)
1 \leq B_j \leq 10^9
(
1 \leq j \leq M
)
入力される値はすべて整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
M
K
X
Y
A_1
\dots
A_N
B_1
\dots
B_M

### 出力

答えを出力せよ。

### 入力例 1

2 3 10
50 6
22 30
20 12 24

### 出力例 1

4
以下の手順で買い物を行うことで、
4
個の商品を買うことができます。
10
ドル紙幣
2
枚を払ってドリンク
1
を買う。お釣りは無し。
10
ドル紙幣
2
枚を払ってドリンク
2
を買う。お釣りは
1
ドル紙幣
8
枚。
10
ドル紙幣
2
枚と
1
ドル紙幣
10
枚を払ってデザート
2
を買う。お釣りは無し。
1
ドル紙幣
22
枚を払ってデザート
1
を買う。お釣りは無し。
4
個よりも多く買うことはできないので、
4
が答えとなります。

### 入力例 2

1 7 67
677677677766666 0
777666777
20 12 24 67 67 67 67

### 出力例 2

1
以下の手順で買い物を行うことで、
1
個の商品を買うことができます。
1
ドル紙幣
600000777666777
枚を払ってデザート
1
を買う。お釣りは
1
ドル紙幣
600000000000000
枚。
1
個よりも多く買うことはできないので、
1
が答えとなります。

### 入力例 3

20 20 30
605776135 133105105
97363214 218434035 697895427 109255624 299037330 227873982 195540071 411713803 828357845 244535208 138059186 639510883 39844882 707397687 371274487 696536603 351588202 319490007 47121612 87169661
32256972 567982330 554885983 299718223 443859449 687952877 264684780 666659381 576335424 941894234 406248934 321334900 423472560 863738035 213143887 384834384 468161291 673106162 164648316 15903323

### 出力例 3

22

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **❌ WA** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 158 ms |
| 提出リンク | [#79387062](https://atcoder.jp/contests/abc476/submissions/79387062) |

<details><summary>提出コード</summary>

```python
N,M,K = map(int,input().split())
x,y = map(int,input().split())
A = list(map(int,input().split()))
B = list(map(int,input().split()))

A.sort()
B.sort()

count = 0
for i in range(M):
  syou, amari = divmod(B[i], K)
  
  if syou <=  y and amari == 0:
    y -= syou
    count += 1
   
  elif (syou+1) <= y and amari != 0:
    y -= (syou + 1)
    oturi = ((syou + 1) * K ) - B[i]
    x += oturi
    count += 1
  else:
    break
 

x += y * K
for j in range(N):

  if A[j] <= x:
    x -= A[j]
    count += 1
  else:
    break
  
 
print(count)

```

</details>

---

### 解説

提出されたコードはD - AutomatEditorial問題でWA（不正解）となりました。
この問題は、Kドル紙幣と1ドル紙幣の2種類の紙幣を用いて、デザートとドリンクをできるだけ多く購入する最大値問題です。

## WA の原因分析

提出コードのWAの主な原因は、**商品の購入戦略が最適ではない**ことにあります。

具体的には、以下の点が問題です。

1.  **Kドル紙幣の扱いに関する誤解**:
    提出コードでは、ドリンクを買い終えた後、`x += y * K` という行で、残りのKドル紙幣 `y` 枚を全て1ドル紙幣に換算してしまっています。
    問題文には「デザート販売機は1ドル紙幣とKドル紙幣の両方を受け入れます」と明記されています。Kドル紙幣をデザート購入時に直接使用できるにも関わらず、一方的に1ドル紙幣に換算してしまうと、支払い戦略の柔軟性が失われます。
    実際には、Kドル紙幣は1ドル紙幣として換算できるだけでなく、Kドル紙幣をそのまま支払いに使うことで、必要な支払額（Kの倍数）に対してぴったり支払える場合など、1ドル紙幣での支払いよりも効率的になるケースがあり得ます。しかし、本問題の場合、お釣りは全て1ドル紙幣で返されるため、Kドル紙幣1枚は常にKドル分の1ドル紙幣の価値を持つと考えられます。よって、デザート購入に

---
