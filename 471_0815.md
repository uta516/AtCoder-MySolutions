# ABC471 解法レポート

**日付**: 2026年08月15日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC471](https://atcoder.jp/contests/abc471)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題C: C - Cookies and Greedy TakahashiEditorial

### 問題文

数直線上の
N
箇所にクッキーが落ちています。
i
番目のクッキーの落ちている座標は
A_i
です。
高橋君は最初、数直線の座標
0
にいて、
N
個のクッキー全てを拾うまで以下の行動を繰り返します。
行動：自身から最も近いクッキーのある座標(複数あるときは座標の最も小さいもの)へ移動し、そのクッキーを拾う。
全てのクッキーを拾うまでの高橋君の移動距離の合計を求めてください。

### 制約

1 \leq N \leq 3\times 10^5
-10^9 \leq A_i \leq 10^9
A_i\neq 0
A_i
は相異なる
入力は全て整数である

### 入力

入力は以下の形式で標準入力から与えられる。
N
A_1
\dots
A_N

### 出力

答えを出力せよ。

### 入力例 1

4
-1 -4 2 -11

### 出力例 1

23
高橋君は以下のように行動します。
座標
0
から座標
-1
へ移動しクッキーを拾う。移動距離は
1
である。
座標
-1
から座標
-4
へ移動しクッキーを拾う。移動距離は
3
である。
座標
-4
から座標
2
へ移動しクッキーを拾う。移動距離は
6
である。
座標
2
から座標
-11
へ移動しクッキーを拾う。移動距離は
13
である。
よって、移動距離の合計は
1+3+6+13=23
となります。
2
回目の行動において、座標
-4
にあるクッキーと座標
2
にあるクッキーまでの距離はともに
3
ですが、高橋君は座標の小さい
-4
の方へ移動します。

### 入力例 2

10
1 2 3 4 5 -1 -2 -3 -4 -6

### 出力例 2

17

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 487 ms |
| 提出リンク | [#78439645](https://atcoder.jp/contests/abc471/submissions/78439645) |

<details><summary>提出コード</summary>

```python
from sortedcontainers import SortedList

N = int(input())
A = list(map(int, input().split()))
A = SortedList(A)

def find(A, target):
    pos = A.bisect_left(target)
    candidates = []
    if pos > 0:
        candidates.append((pos-1, A[pos-1]))
    if pos < len(A):
        candidates.append((pos, A[pos]))
    return min(candidates, key=lambda x: abs(x[1] - target))

target = 0
count = 0
for i in range(N):
    pos, closest = find(A, target)
    A.pop(pos)
    count += abs(target - closest)
    target = closest
print(count)
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、高橋君が数直線上でクッキーを拾っていくときの総移動距離を求めるものです。高橋君は常に**自身から最も近いクッキー**を選び、複数ある場合は**座標の最も小さいもの**を選びます。

この問題を解くための主要な課題は、効率的に「最も近いクッキー」を見つけ、拾ったクッキーをリストから削除することです。
通常のPythonリスト (`list`) は、要素の検索（特定の値を`O(N)`、ソート済みリストでの二分探索は`bisect`モジュールを使えば`O(logN)`）や、任意の位置からの要素削除（`list.pop(index)`は`O(N)`）に時間がかかります。
N個のクッキーを拾うためにN回この操作を繰り返すと、全体で$O(N^2)$の計算量となり、$N=3 \times 10^5$の制約では時間切れ（TLE）になってしまいます。

そこで、Pythonの標準ライブラリでは提供されていないが競技プログラミングでよく用いられる外部ライブラリ `sortedcontainers` の `SortedList` を使用します。`SortedList` は要素を常にソートされた状態で保持し、以下の操作を効率的に行うことができます。
- 要素の挿入: $O(\log K)$
- 要素の削除: $O(\log K)$
- 要素の検索（二分探索）：$O(\log K)$
ここで $K$ はリストの現在の要素数です。

**アルゴリズムの手順:**

1.  **初期化:**
    *   クッキーの座標リスト `A` を `SortedList` として初期化します。これにより、クッキーは常にソートされた状態に保たれます。
    *   高橋君の現在の座標 `current_takahashi_pos` を `0` に設定します。
    *   総移動距離 `total_distance` を `0` に設定します。

2.  **クッキーを拾うループ:**
    *   `N` 回繰り返します（全てのクッキーを拾うまで）。
    *   **最も近いクッキーの探索:**
        *   `SortedList` の `bisect_left(current_takahashi_pos)` メソッドを使用して、`current_takahashi_pos` 以上の最初の要素が挿入されるべきインデックス `pos` を取得します。
        *   この `pos` に基づいて、`current_takahashi_pos` に最も近い候補となるクッキーを最大2つ見つけます。
            *   `pos > 0` の場合: `sorted_list_A[pos - 1]` は `current_takahashi_pos` より小さい最大のクッキーです。これを候補に追加します。
            *   `pos < len(sorted_list_A)` の場合: `sorted_list_A[pos]` は `current_takahashi_pos` 以上の最小のクッキーです。これを候補に追加します。
        *   これらの候補の中から、`current_takahashi_pos` からの距離が最小のクッキーを選びます。もし距離が同じ場合は、問題の指示通り**座標が小さい方**を選びます。これは `min()` 関数の `key` 引数に `(abs(距離), 座標)` のタプルを指定することで実現できます。
    *   **移動と距離の更新:**
        *   選ばれたクッキーの座標を `closest_cookie_coord` とします。
        *   `total_distance` に `abs(current_takahashi_pos - closest_cookie_coord)` を加算します。
    *   **クッキーの削除と現在地の更新:**
        *   `SortedList` から `closest_cookie_coord` に対応するクッキーを削除します（`pop(index)`を使用）。
        *   `current_takahashi_pos` を `closest_cookie_coord` に更新します。

3.  **結果の出力:**
    *   ループ終了後、`total_distance` を出力します。

## 計算量

*   **時間計算量: O(N log N)**
    *   `SortedList` の初期化: `O(N log N)`
    *   メインループが `N` 回繰り返されます。
    *   各ループ内で:
        *   `bisect_left` による探索: `O(log K)` （$K$は現在のリストサイズ、最大で$N$）
        *   `min` 関数による候補の選択: 定数時間 ($2$つの候補しかないため)
        *   `SortedList.pop`: `O(log K)`
    *   したがって、合計で `O(N log N + N * (log K + log K)) = O(N log N)` となります。

*   **空間計算量: O(N)**
    *   `SortedList` が `N` 個のクッキー座標を保持するため、`O(N)` の空間が必要です。

## Python 模範解答

```python
from sortedcontainers import SortedList

# N個のクッキーの数を入力
N = int(input())
# クッキーの座標リストAを入力し、SortedListとして初期化
# SortedListは要素を常にソートされた状態で保持し、O(logN)で検索・挿入・削除が可能
A = SortedList(map(int, input().split()))

def find_closest_cookie(sorted_list_A, current_pos):
    """
    現在の位置 (current_pos) から最も近いクッキーを探す関数。
    複数のクッキーが同じ最短距離にある場合、座標が小さい方を優先する。
    """
    # bisect_leftはcurrent_pos以上の最初の要素が挿入されるべきインデックスを返す
    # 例: list=[1, 5, 10], current_pos=7 -> pos=2 (10のインデックス)
    pos = sorted_list_A.bisect_left(current_pos)
    
    candidates = []
    
    # current_posより小さい要素が存在する場合、左隣の要素を候補に追加
    # (つまり、current_posに最も近い小さいクッキー)
    if pos > 0:
        # (インデックス, 座標) の形式で候補に追加
        candidates.append((pos - 1, sorted_list_A[pos - 1]))
        
    # current_pos以上の要素が存在する場合、右隣の要素を候補に追加
    # (つまり、current_posに最も近い大きいクッキー、またはcurrent_posそのもの)
    if pos < len(sorted_list_A):
        # (インデックス, 座標) の形式で候補に追加
        candidates.append((pos, sorted_list_A[pos]))
        
    # 候補の中から、最も近いクッキーを選択する。
    # keyにタプル (距離, 座標) を指定することで、
    # 1. 距離 (abs(cookie_coord - current_pos)) が最小になるものを優先
    # 2. 距離が同じ場合は、座標 (cookie_coord) が最小になるものを優先
    # という問題の条件を正確に満たす。
    # sortedcontainers.SortedListからのpopのためにインデックスも保持する。
    return min(candidates, key=lambda x: (abs(x[1] - current_pos), x[1]))

# 高橋君の現在の座標を0で初期化
current_takahashi_pos = 0
# 総移動距離を0で初期化
total_distance = 0

# 全てのクッキーを拾うまでN回繰り返す
for _ in range(N):
    # 現在の場所から最も近いクッキーを見つける
    # closest_cookie_idx: SortedList内でのクッキーのインデックス
    # closest_cookie_coord: そのクッキーの座標
    closest_cookie_idx, closest_cookie_coord = find_closest_cookie(A, current_takahashi_pos)
    
    # 総移動距離に現在の場所からクッキーまでの距離を加算
    total_distance += abs(current_takahashi_pos - closest_cookie_coord)
    
    # 拾ったクッキーをリストから削除
    # SortedListのpopはインデックスを指定して要素をO(logN)で削除できる
    A.pop(closest_cookie_idx)
    
    # 高橋君の現在地を拾ったクッキーの座標に更新
    current_takahashi_pos = closest_cookie_coord

# 計算された総移動距離を出力
print(total_distance)

```

## 実装のポイント

*   **`sortedcontainers.SortedList` の利用:**
    *   Pythonの標準ライブラリには、ソートされた状態を保ちつつ高速な挿入・削除が可能なデータ構造がありません。`bisect` モジュールはソート済みリストでの検索は高速ですが、`list.pop()` は要素を削除する際にその後の全要素をシフトするため $O(N)$ かかります。
    *   この問題のように、ソートされたリストから要素を効率的に検索・削除する必要がある場合、`SortedList` は非常に強力なツールです。AtCoderなどの競技プログラミングプラットフォームでは、通常このライブラリが利用可能です。

*   **最も近いクッキーの探索:**
    *   `SortedList.bisect_left(target)` は、`target` 以上の最初の要素のインデックスを $O(\log K)$ で返します。このインデックスとその一つ前のインデックス（存在する場合）の要素が、`target` に最も近い候補となります。
    *   これにより、リスト全体を走査することなく、効率的に候補を絞り込むことができます。

*   **距離が同じ場合の優先順位:**
    *   問題文に「複数あるときは座標の最も小さいもの」と明確に指示されています。
    *   `min()` 関数の `key` 引数に `lambda x: (abs(x[1] - current_pos), x[1])` のようにタプルを指定することで、この条件を正確に満たすことができます。`min()` はまずタプルの最初の要素（距離）で比較し、それが同じ場合は2番目の要素（座標）で比較して最小値を選択します。

---
