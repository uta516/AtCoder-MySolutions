# ABC458 解法レポート

**日付**: 2026年05月16日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC458](https://atcoder.jp/contests/abc458)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - ChompersEditorial

### 問題文

英小文字からなる文字列
S
と正の整数
N
が与えられます。
S
の長さは
2N+1
以上です。
S
の先頭と末尾から
N
文字ずつ取り除いて得られる文字列を求めてください。

### 制約

S
は英小文字からなる文字列
N
は整数
2N+1 \leq |S| \leq 30
1 \leq N \leq 10

### 入力

入力は以下の形式で標準入力から与えられる。
S
N

### 出力

答えを出力せよ。

### 入力例 1

chemotherapy
3

### 出力例 1

mother
chemotherapy
の先頭
3
文字 (
che
) と末尾
3
文字 (
apy
) を取り除くと、
mother
が得られます。

### 入力例 2

thermometer
4

### 出力例 2

mom

### 入力例 3

burger
1

### 出力例 3

urge

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 44 ms |
| 提出リンク | [#75853890](https://atcoder.jp/contests/abc458/submissions/75853890) |

<details><summary>提出コード</summary>

```python
S = str(input())
N = int(input())

print(S[N:len(S)-N])
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられた文字列 `S` から先頭と末尾の `N` 文字ずつを取り除いた新しい文字列を求めるものです。Pythonのような多くのプログラミング言語では、文字列から部分文字列を抽出する「スライス」機能が提供されています。

このスライス機能を利用して、以下の範囲の文字列を抽出します。
1.  **先頭の `N` 文字を取り除く**: これは、元の文字列のインデックス `N` から抽出を開始することを意味します。
2.  **末尾の `N` 文字を取り除く**: これは、元の文字列の長さが `len(S)` である場合、インデックス `len(S) - N` で抽出を終了することを意味します（Pythonのスライスでは終了インデックスは含まれないため、この値が適切です）。

したがって、文字列 `S` からインデックス `N` からインデックス `len(S) - N` の直前までの部分を抽出することで、目的の文字列が得られます。

## 計算量

-   **時間計算量**: `O(|S|)`
    Pythonの文字列スライスは、新しい文字列を生成するために、スライスされる部分の長さに比例する時間を要します。今回の問題では、文字列 `S` の長さ `|S|` が最大30と非常に小さいため、実質的には定数時間 `O(1)` と見なすこともできます。
-   **空間計算量**: `O(|S|)`
    スライスによって新しい文字列が生成されるため、その文字列の長さに比例するメモリが必要となります。これも `|S|` が最大30であるため、実質的には定数空間 `O(1)` と見なすことができます。

## Python 模範解答

```python
# 標準入力から文字列 S を読み込む
S = str(input())
# 標準入力から整数 N を読み込む
N = int(input())

# 文字列 S の先頭から N 文字、末尾から N 文字を取り除いた部分文字列を抽出する
# Pythonのスライス S[start:end] は、インデックス start から end-1 までの部分文字列を返す
# 先頭 N 文字を取り除くため、開始インデックスは N
# 末尾 N 文字を取り除くため、終了インデックスは len(S) - N
# （len(S) - N で指定されたインデックスは含まれず、その直前までが抽出される）
result = S[N : len(S) - N]

# 結果を出力する
print(result)
```

## 実装のポイント

*   **Pythonの文字列スライス `S[start:end]` の理解**:
    *   `start` で指定されたインデックスの文字は結果に含まれます。
    *   `end` で指定されたインデックスの文字は結果に含まれません（`end-1` までの文字が抽出されます）。
    *   これにより、「先頭からN文字」を取り除くには `start=N`、「末尾からN文字」を取り除くには `end=len(S)-N` と指定するのが直感的かつ正しいです。
*   **制約の確認**:
    *   問題文の制約 `2N+1 \leq |S|` は、抽出される文字列が必ず空ではないことを保証しています。具体的には、`len(S) - 2N \geq 1` となり、常に1文字以上の文字列が残ります。このため、スライスが不正な範囲になる心配はありません。
    *   `|S| \leq 30` および `N \leq 10` といった非常に小さい制約は、どのようなアルゴリズムを用いても実行時間に問題がないことを示唆していますが、スライスを用いる方法が最も簡潔かつ効率的です。

---

## 問題B: B - Count Adjacent CellsEditorial

### 問題文

H
行
W
列のグリッドがあります。上から
i
行目、左から
j
列目のマスをマス
(i,j)
と表します。
マス
(x_1, y_1)
とマス
(x_2, y_2)
が
辺で隣接する
とは、
|x_1 - x_2| + |y_1 - y_2| = 1
が成り立つことをいいます。
すべてのマスについて、そのマスに辺で隣接するマスの個数を求めてください。

### 制約

1 \leq H,W \leq 50
入力される値はすべて整数

### 入力

入力は以下の形式で標準入力から与えられる。
H
W

### 出力

答えを以下の形式で出力せよ。
x_{1,1}
x_{1,2}
\cdots
x_{1,W}
x_{2,1}
x_{2,2}
\cdots
x_{2,W}
\vdots
x_{H,1}
x_{H,2}
\cdots
x_{H,W}
ここで、
x_{i,j}
はマス
(i,j)
に辺で隣接するマスの個数を表す。

### 入力例 1

4 5

### 出力例 1

2 3 3 3 2
3 4 4 4 3
3 4 4 4 3
2 3 3 3 2
マス
(1, 5)
に辺で隣接するマスは、マス
(1, 4), (2, 5)
の
2
個です。
マス
(2, 3)
に辺で隣接するマスは、マス
(1, 3), (2, 2), (2, 4), (3, 3)
の
4
個です。
マス
(4, 2)
に辺で隣接するマスは、マス
(3, 2), (4, 1), (4, 3)
の
3
個です。

### 入力例 2

1 1

### 出力例 2

0
マス
(1,1)
に辺で隣接するマスは存在しません。

### 入力例 3

12 8

### 出力例 3

2 3 3 3 3 3 3 2
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
3 4 4 4 4 4 4 3
2 3 3 3 3 3 3 2

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 58 ms |
| 提出リンク | [#75888520](https://atcoder.jp/contests/abc458/submissions/75888520) |

<details><summary>提出コード</summary>

```python

H,W = map(int,input().split())

ans=[[0] * (W) for _ in range(H)]

if H ==1 :
  if W == 1:
    print(0)
  elif W == 2:
    print(1,1)
  else:
    for i in range(W):
      
      if i == 0 or i == W-1:
        ans[0][i] = 1
      else:
        ans[0][i] = 2
      
    for line in ans :
      print(" ".join(map(str,line)))
elif H == 2:
  if W == 1:
    print(1)
    print(1)
  elif W == 2:
    for i in range(2):
      print(2,2)
  else:
    for i in range(H):
      for j in range(W):
        if j == 0 or j == W-1:
          ans[i][j]=2
        else:
          ans[i][j]=3
    for line in ans :
      print(" ".join(map(str,line)))
elif W == 1:
  for i in range(H):
    if i == 0 or i == H-1:
      ans[i][0] = 1
    else:
      ans[i][0] = 2
  for line in ans :
    print(" ".join(map(str,line)))
    
else:
  for i in range(H):
    for j in range(W):
      if i == 0:
        if j == 0 or j == W-1:
          ans[i][j]=2
        else:
          ans[i][j]=3
      elif i == H-1:
        if j == 0 or j == W-1:
          ans[i][j]=2
        else:
          ans[i][j]=3
      else:
        if j == 0 or j == W-1:
          ans[i][j]=3
        else:
          ans[i][j]=4

  count = 0
  for line in ans :
    print(" ".join(map(str,line)))
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、H行W列のグリッド上の各マスについて、そのマスに「辺で隣接する」マスの個数を求めるものです。2つのマス $(x_1, y_1)$ と $(x_2, y_2)$ が辺で隣接するとは、マンハッタン距離 $|x_1 - x_2| + |y_1 - y_2| = 1$ が成り立つことを指します。これは、上下左右のいずれかの方向に隣接していることを意味します。

各マス $(i, j)$ (問題文では1-indexedですが、プログラミングでは0-indexedで考えます) に対して、隣接するマスは最大で以下の4つです。
1. 上のマス: $(i-1, j)$
2. 下のマス: $(i+1, j)$
3. 左のマス: $(i, j-1)$
4. 右のマス: $(i, j+1)$

これらの潜在的な隣接マスが、実際にグリッドの範囲内に存在するかどうかをチェックし、存在するマスの数を数え上げればよいです。

具体的には、各マス $(i, j)$ (0-indexed) について、以下の条件が真であれば隣接するマスが存在すると判断し、カウントを1増やします。
- **上のマス:** `i > 0` (つまり、現在の行が0行目ではない場合)
- **下のマス:** `i < H - 1` (つまり、現在の行が最終行ではない場合)
- **左のマス:** `j > 0` (つまり、現在の列が0列目ではない場合)
- **右のマス:** `j < W - 1` (つまり、現在の列が最終列ではない場合)

このシンプルなロジックをグリッドのすべてのマスに適用することで、各マスの隣接数を効率的に計算できます。H=1, W=1 のような特殊なケースも、この統一的なロジックで正しく処理されます。

## 計算量

- **時間計算量**: O(H * W)
  H行W列のグリッドのすべてのマスを一度ずつ走査します。各マスに対して、定数回（最大4回）の条件分岐と加算を行うため、全体の計算量はグリッドのサイズに比例します。
  制約が $H, W \le 50$ なので、最大で $50 \times 50 = 2500$ 回程度の非常に高速な処理で完了します。

- **空間計算量**: O(H * W)
  結果を格納するために、H行W列の二次元配列（グリッド）が必要です。

## Python 模範解答

```python
H, W = map(int, input().split())

# 結果を格納するH行W列の二次元配列を0で初期化
# Pythonのリスト内包表記で効率的に作成
ans = [[0] * W for _ in range(H)]

# グリッドの各マス (i, j) (0-indexed) について処理
for i in range(H):
    for j in range(W):
        count = 0  # 現在のマス (i, j) の隣接マス数を初期化
        
        # 上方向のマス (i-1, j) がグリッド内に存在するかチェック
        # iが0より大きい場合、上の行が存在する
        if i > 0:
            count += 1
        
        # 下方向のマス (i+1, j) がグリッド内に存在するかチェック
        # iがH-1より小さい場合、下の行が存在する
        if i < H - 1:
            count += 1
        
        # 左方向のマス (i, j-1) がグリッド内に存在するかチェック
        # jが0より大きい場合、左の列が存在する
        if j > 0:
            count += 1
        
        # 右方向のマス (i, j+1) がグリッド内に存在するかチェック
        # jがW-1より小さい場合、右の列が存在する
        if j < W - 1:
            count += 1
            
        # 計算した隣接マス数を結果配列に格納
        ans[i][j] = count

# 結果の出力
# 各行の要素をスペース区切りで連結し、改行して出力
for row in ans:
    print(" ".join(map(str, row)))

```

## 実装のポイント

-   **0-indexedの活用**: 競技プログラミングでは、配列やグリッドを扱う際に0-indexed (インデックスが0から始まる) で考えるのが一般的で、Pythonの `range()` 関数とも相性が良いです。問題文が1-indexedであっても、内部処理は0-indexedで行い、必要に応じて変換する方がシンプルになることが多いです。
-   **境界条件の処理**: グリッドの端や角にあるマスでは、隣接するマスがグリッドの外に出てしまうため、その方向には隣接するマスは存在しません。模範解答のように `i > 0`, `i < H - 1`, `j > 0`, `j < W - 1` といった条件で存在チェックを行うことで、すべてのケース（角、辺、内部）を統一的に処理できます。
-   **出力形式**: 各行の要素をスペースで区切り、行ごとに改行して出力する必要があります。Pythonでは `map(str, row)` で数値のリストを文字列のリストに変換し、`" ".join(...)` でスペース区切り文字列を生成するのが標準的で効率的な方法です。
-   **特殊ケースの対応**: H=1やW=1といったグリッドのサイズが非常に小さい場合のケースも、この汎用的なロジックでカバーされます。たとえば `H=1, W=1` の場合、すべての `if` 条件が偽となるため `count` は `0` となり、正しく処理されます。

---
