# ABC475 解法レポート

**日付**: 2026年09月13日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC475](https://atcoder.jp/contests/abc475)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - mnclrEditorial

### 問題文

英小文字からなる文字列
S
が与えられます。
S
の隣り合う文字の間にそれぞれ
o
を挿入してできる文字列を出力してください。

### 制約

S
は英小文字のみからなる長さ
2
以上
10
以下の文字列

### 入力

入力は以下の形式で標準入力から与えられる。
S

### 出力

答えを出力せよ。

### 入力例 1

mtr

### 出力例 1

motor

### 入力例 2

mnclr

### 出力例 2

monocolor

### 入力例 3

oo

### 出力例 3

ooo

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 46 ms |
| 提出リンク | [#79185305](https://atcoder.jp/contests/abc475/submissions/79185305) |

<details><summary>提出コード</summary>

```python
S = str(input())

N = len(S)
result = []
for i in range(N):
  if i == N-1:
    result.append(S[i])
  else:
    result.append(S[i] + "o")

print("".join(result))
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられた英小文字の文字列 `S` に対して、隣接する文字の間にそれぞれ文字 `'o'` を挿入し、その結果の文字列を出力するというものです。

例えば、`S = "mtr"` であれば、`m` と `t` の間に `'o'`、`t` と `r` の間に `'o'` を挿入して `"motor"` を生成します。

この処理を実現する基本的な考え方は以下の通りです。
1. 文字列 `S` の各文字を順に取り出します。
2. 取り出した文字を結果の文字列に追加します。
3. もしその文字が `S` の最後の文字でなければ、その文字の直後に `'o'` を結果の文字列に追加します。
4. この手順を `S` のすべての文字について繰り返すことで、目的の文字列が完成します。

Pythonでは、`str.join()` メソッドを利用すると、この操作を非常に簡潔かつ効率的に記述できます。`"separator".join(iterable)` は、`iterable`（例えば文字列のリストやタプル、または今回の `S` のような文字列自体）の各要素を `separator` で連結した新しい文字列を生成します。この問題では、`S` の各文字が `iterable` の要素となり、`'o'` が `separator` となるため、`"o".join(S)` という表現が直接的に求める結果の文字列を生成します。

## 計算量
-   **時間計算量**: O(N)
    `N` を文字列 `S` の長さとします。
    `"o".join(S)` メソッドは、文字列 `S` の各文字を一度ずつ処理し、`N-1` 回の `'o'` の挿入と `N` 文字の結合を行います。Pythonの文字列結合操作は内部的に最適化されており、全体として `N` に比例する時間で完了します。したがって、時間計算量は O(N) です。

-   **空間計算量**: O(N)
    `"o".join(S)` によって生成される新しい文字列の長さは、元の文字数 `N` と挿入される `'o'` の数 `(N-1)` の合計、つまり `2N - 1` です。この新しい文字列を格納するために必要な空間は `N` に比例するため、空間計算量は O(N) です。

## Python 模範解答

```python
# 標準入力から文字列Sを読み込む
# input()はデフォルトで文字列を返すため、str()による明示的な型変換は不要
S = input()

# Pythonのstr.join()メソッドを使用するのが最も簡潔で効率的です。
# Sの各文字間に"o"を挿入した新しい文字列を生成します。
result = "o".join(S)

# 結果の文字列を出力する
print(result)
```

## 実装のポイント
-   **`str.join()` メソッドの活用**: Pythonにおいて、複数の文字列を特定の区切り文字で連結したい場合、`separator.join(iterable)` メソッドが最も推奨される方法です。これは、ループ内で `+` 演算子を使って文字列を繰り返し結合するよりも、一般的に高速でメモリ効率も良いです。
-   **入力の読み込み**: `input()` 関数は標準入力から1行を読み込み、その内容を文字列として返します。したがって、`str(input())` のように明示的に `str()` で変換する必要はありません。
-   **制約の考慮**: 問題の制約では `S` の長さが2以上10以下と非常に短いため、どのような素朴な実装（例：ループと文字列結合）でも実行時間の上限に達することはありません。しかし、より大規模な入力に対しても通用する効率的でPythonicな方法を選ぶことは、良いプログラミング習慣です。

---

## 問題B: B - ChangeEditorial

### 問題文

AtCoder王国では、
1
円硬貨、
10
円硬貨、
100
円硬貨の
3
種類の硬貨と、
1000
円紙幣が流通しています。
高橋君は
1000
円紙幣を
10^{100}
枚、硬貨を
0
枚持っている状態から買い物を
N
回行いました。
i
回目の買い物では
A_i
円の商品を購入しました。
支払いでは、合計金額が
A_i
円以上になるような最小枚数の
1000
円紙幣のみを出し、お釣りの硬貨の合計枚数が最小となるようにお釣りを受け取りました。
N
回の買い物が全て終わった時点で高橋君が持っている各硬貨の枚数を求めてください。

### 制約

1\leq N \leq 1000
1\leq A_i \leq 10^5
入力は全て整数である

### 入力

入力は以下の形式で標準入力から与えられる。
N
A_1
\dots
A_N

### 出力

N
回の買い物が終わった時点で高橋君が持っている
1
円硬貨、
10
円硬貨、
100
円硬貨の枚数を、この順に空白区切りで出力せよ。

### 入力例 1

3
1296 110 1

### 出力例 1

13 18 24
1
回目の買い物で、高橋君は
1296
円の支払いに対し
1000
円紙幣
2
枚を出し、
100
円硬貨
7
枚と
1
円硬貨
4
枚をお釣りとして受け取ります。
2
回目の買い物で、高橋君は
110
円の支払いに対し
1000
円紙幣
1
枚を出し、
100
円硬貨
8
枚と
10
円硬貨
9
枚をお釣りとして受け取ります。
3
回目の買い物で、高橋君は
1
円の支払いに対し
1000
円紙幣
1
枚を出し、
100
円硬貨
9
枚、
10
円硬貨
9
枚、
1
円硬貨
9
枚をお釣りとして受け取ります。
3
回の買い物が終わった時点で、高橋君は
1
円硬貨を
13
枚、
10
円硬貨を
18
枚、
100
円硬貨を
24
枚持っています。

### 入力例 2

12
3141 592 65358 9 79 323 84 6264 3 38327 950 28

### 出力例 2

52 59 82

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 53 ms |
| 提出リンク | [#79202275](https://atcoder.jp/contests/abc475/submissions/79202275) |

<details><summary>提出コード</summary>

```python
N = int(input())
A = list(map(int,input().split()))

total_1=0
total_10=0
total_100=0

for i in range(N):
 sen = A[i]//1000
 insert_price = 1000 * (sen+1)
 product_price = A[i]
 change = int(insert_price) - int(product_price)

 cahnge_1000 = change//1000     
 change = change%1000
 cahnge_100 = change//100        # 百円玉の枚数
 change = change%100
 cahnge_10 = change//10           # 十円玉の枚数
 change = change%10
 cahnge_1 = change//1              # 一円玉の枚数
 
 total_1 += cahnge_1
 total_10 += cahnge_10
 total_100 += cahnge_100

print(total_1,total_10,total_100)
  
```

</details>

---

### 解説

AtCoderのB - Change問題の解説です。

## アルゴリズム解説

この問題では、高橋君が行う $N$ 回の買い物を通して、最終的に彼が持っている1円硬貨、10円硬貨、100円硬貨の総枚数を求めます。各買い物は独立して処理され、そのたびに高橋君がお釣りとして受け取る硬貨の枚数を合計していきます。

各買い物 ($A_i$ 円の商品) について、以下の2つのステップを繰り返します。

1.  **支払い金額の決定**:
    *   高橋君は「合計金額が $A_i$ 円以上になるような最小枚数の 1000 円紙幣」で支払います。
    *   問題文のこの部分には解釈の余地がありますが、入出力例1の挙動（1296円に対し2000円を支払う）から、**常に商品の値段を1000円単位で切り上げた次の額（ただし、ちょうど1000の倍数の場合は、その額に1000円足した額）を支払う**と解釈するのが正しいです。
    *   具体的には、支払い金額 `paid_amount` は `(A_i // 1000 + 1) * 1000` で計算されます。
        *   例: $A_i = 1296$ の場合、$1296 // 1000 = 

---
