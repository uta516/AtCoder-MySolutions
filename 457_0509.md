# ABC457 解法レポート

**日付**: 2026年05月12日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC457](https://atcoder.jp/contests/abc457)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - ArrayEditorial

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 52 ms |
| 提出リンク | [#75616334](https://atcoder.jp/contests/abc457/submissions/75616334) |

## アルゴリズム解説
この問題は、与えられた数列 `A` の指定されたインデックス `X` の要素を出力するという、非常に基本的な配列（リスト）の操作を問うものです。

1.  **入力の読み込み**: まず、数列の長さ `N`、次に数列 `A` そのもの、そして最後に指定インデックス `X` をそれぞれ標準入力から読み込みます。
2.  **インデックスの調整**: 問題文では数列 `A` は `A_1, A_2, \cdots, A_N` という1-based index（1始まり）で表現されています。しかし、Pythonのリストや多くのプログラミング言語の配列は0-based index（0始まり）を採用しています。つまり、`A_1` はリストの `A[0]`、`A_2` は `A[1]` となります。したがって、問題で求められている `A_X` の値は、Pythonのリストでは `A[X-1]` に対応します。
3.  **値の出力**: 調整したインデックス `X-1` を使ってリスト `A` から値を取り出し、それを出力します。

## 計算量
-   **時間計算量**: $O(N)$
    -   `N`, `X` の読み込みは $O(1)$ です。
    -   数列 `A` を読み込み、リストに格納する処理は、`N` 個の整数を処理するため $O(N)$ となります。
    -   リストの特定のインデックス `A[X-1]` にアクセスする処理は $O(1)$ です。
    -   全体として、数列 `A` の読み込みが支配的であるため、$O(N)$ となります。
-   **空間計算量**: $O(N)$
    -   数列 `A` の値をリストとして保存するために $N$ 個の整数分のメモリが必要となるため、$O(N)$ となります。
    -   `N` の最大値が100と非常に小さいため、これらの計算量は全く問題ありません。

## Python 模範解答

```python
# Nを読み込む
N = int(input())

# 数列Aを読み込む
# input().split() で空白区切りの文字列をリストにし、
# map(int, ...) でそれぞれの文字列を整数に変換し、
# list(...) で最終的に整数のリストAを作成
A = list(map(int, input().split()))

# Xを読み込む
X = int(input())

# A_X の値を出力する
# Pythonのリストは0-based indexなので、X-1 でアクセスする
print(A[X-1])
```

## 実装のポイント

*   **1-based index と 0-based index の変換**: 最も重要なポイントです。問題文は `A_X` と1-based indexで指定していますが、Pythonのリストは `A[0], A[1], ...` と0-based indexです。このため、`X` 番目の要素にアクセスするには `A[X-1]` とする必要があります。競プロでは非常によくある注意点なので、常に意識することが大切です。
*   **効率的な入力処理**: Pythonで空白区切りの整数列をリストとして読み込む際には、`list(map(int, input().split()))` というイディオムが非常に一般的で効率的です。各要素を個別に読み込むよりも高速です。
*   **制約の確認**: `N` の最大値が100と非常に小さいため、リストの読み込みやアクセスにかかる時間、リストを格納するメモリなどは全く心配ありません。もし `N` が $10^5$ や $10^6$ といった大きな値であれば、線形時間 $O(N)$ のアルゴリズムが求められることになりますが、この問題では現在の解法で十分最適です。

---

## 問題B: B - ArraysEditorial

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 239 ms |
| 提出リンク | [#75629486](https://atcoder.jp/contests/abc457/submissions/75629486) |

## アルゴリズム解説

この問題は、与えられた複数の数列の中から、指定されたインデックスの要素を取り出して出力する、というシンプルなデータアクセス問題です。

1.  **データの読み込み**: まず、`N` 個の数列が与えられます。各数列は、その長さ `L_i` と、その後に続く `L_i` 個

---

## 問題C: C - Long SequenceEditorial

| 項目 | 内容 |
|------|------|
| 提出結果 | **❌ RE** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 874 ms |
| 提出リンク | [#75668684](https://atcoder.jp/contests/abc457/submissions/75668684) |

## RE の原因分析

提出されたコードが RE (Runtime Error) になった主な原因は、**メモリの不足**です。

問題の制約を確認すると、以下の値が非常に大きくなる可能性があります。
*   `C_i` は最大 `10^9`。
*   `sum L_i` は最大 `2 \times 10^

---
