# ABC457 解法レポート

**日付**: 2026年05月12日  
**ユーザー**: [hiyokosann](https://atcoder.jp/users/hiyokosann)  
**コンテスト**: [ABC457](https://atcoder.jp/contests/abc457)

> このレポートは GitHub Actions により自動生成されました。

---

## 問題A: A - ArrayEditorial

### 問題文

長さ
N
の数列
A = (A_1, A_2, \cdots , A_N)
が与えられます。
その後に
1
以上
N
以下の整数
X
が与えられます。
A_X
の値を出力してください。

### 制約

1 \le X \le N \le 100
1 \le A_i \le 100
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
A_1
A_2
\ldots
A_N
X

### 出力

A_X
の値を出力せよ。

### 入力例 1

5
1 2 3 4 5
3

### 出力例 1

3
(A_1, A_2, A_3, A_4 , A_5) = (1, 2, 3, 4, 5)
です。
A_3 = 3
のため
3
と出力してください。

### 入力例 2

10
6 6 9 6 10 5 7 2 8 2
4

### 出力例 2

6

### 入力例 3

10
4 4 4 3 4 2 1 1 2 1
10

### 出力例 3

1

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 52 ms |
| 提出リンク | [#75616334](https://atcoder.jp/contests/abc457/submissions/75616334) |

<details><summary>提出コード</summary>

```python
N =int(input())
A = list(map(int,input().split()))
X = int(input())

print(A[X-1])
```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられた数列 `A` の `X` 番目の要素の値を単純に出力することを求めています。

1.  **入力の読み込み**: まず、数列の長さ `N`、次にスペース区切りの整数列として数列 `A` の要素、最後に取得したい要素のインデックス `X` をそれぞれ読み込みます。
2.  **数列の格納**: 読み込んだ数列 `A` の要素をPythonのリストとして格納します。
3.  **要素の取得と出力**: 問題文では数列の要素が `A_1, A_2, \cdots, A_N` のように1から始まるインデックス（1-indexed）で表記されています。しかし、Pythonのリストは `A[0], A[1], \cdots, A[N-1]` のように0から始まるインデックス（0-indexed）を使用します。したがって、`A_X` を取得するためには、Pythonリストの `X-1` 番目の要素にアクセスする必要があります。この値を取得し、標準出力に出力します。

## 計算量

-   **時間計算量**:
    -   `N` の読み込み: O(1)
    -   数列 `A` の読み込みとリストへの格納: `N` 個の要素を読み込むため、O(N)
    -   `X` の読み込み: O(1)
    -   `A[X-1]` へのアクセス: リストの要素への直接アクセスは O(1)
    -   合計: 数列 `A` の読み込みが支配的なため、**O(N)**

-   **空間計算量**:
    -   数列 `A` をPythonのリストとして格納するため、`N` 個の整数を保持する必要があります。そのため、**O(N)** の空間が必要です。

今回の制約 `N <= 100` において、O(N) の時間計算量・空間計算量は非常に高速であり、全く問題ありません。

## Python 模範解答

```python
# N: 数列の長さ (1 <= N <= 100)
N = int(input())

# A: 数列 A_1, A_2, ..., A_N
# input().split() で標準入力から読み込んだ行をスペースで分割し、文字列のリストにする
# map(int, ...) でその文字列リストの各要素を整数に変換する
# list(...) で変換された整数を要素とするリストを作成する
A = list(map(int, input().split()))

# X: 取得したい要素のインデックス (1-indexed, 1 <= X <= N)
X = int(input())

# Pythonのリストは0-indexed (0から始まるインデックス) です。
# 問題文の A_X は、Pythonリストの A[X-1] に対応します。
# 例: A_1 は A[0]、A_2 は A[1]、...、A_X は A[X-1]
print(A[X-1])
```

## 実装のポイント

*   **インデックスの変換 (1-indexed to 0-indexed)**: 最も重要なポイントです。問題文では数学的な慣習に従い1から始まるインデックス (`A_1, A_2, \ldots, A_N`) が使われますが、Pythonのリスト（および多くのプログラミング言語の配列）は0から始まるインデックス (`A[0], A[1], \ldots, A[N-1]`) を使います。したがって、`X` 番目の要素にアクセスするには `X-1` というインデックスを指定する必要があります。
*   **効率的な入力読み込み**:
    *   `N = int(input())` や `X = int(input())` は1行に1つの整数が与えられる場合に標準的な方法です。
    *   `A = list(map(int, input().split()))` は、スペース区切りで複数の整数が1行に与えられる場合に、それらを整数リストとして一度に読み込むためのPythonのイディオムとして非常に効率的でよく使われます。
*   **制約の確認**: `1 <= X <= N` という制約があるため、`X-1` は必ず `0` から `N-1` の範囲内に収まります。これにより、リストのインデックスが範囲外になる `IndexError` の心配はありません。A問題のような基礎的な問題では、この手の範囲外アクセスは考慮する必要がないことが多いですが、より複雑な問題では注意が必要です。

---

## 問題B: B - ArraysEditorial

### 問題文

N
個の数列
A_1, A_2, \ldots, A_N
が与えられます。
数列
A_i
の長さは
L_i
であり
A_i = (A_{i,1}, A_{i,2}, \ldots, A_{i,L_i})
です。
その後、整数
X, Y
が与えられます。
A_{X,Y}
の値を出力してください。

### 制約

1 \le N \le 2 \times 10^5
1 \le L_i
L_i
の総和は
2 \times 10^5
以下
1 \le A_{i,j} \le 1000
1 \le X \le N
1 \le Y \le L_X
入力される値はすべて整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
L_1
A_{1,1}
A_{1,2}
\ldots
A_{1,L_1}
L_2
A_{2,1}
A_{2,2}
\ldots
A_{2,L_2}
\vdots
L_N
A_{N,1}
A_{N,2}
\ldots
A_{N,L_N}
X
Y

### 出力

A_{X,Y}
の値を出力せよ。

### 入力例 1

3
3 10 20 30
1 7
4 5 6 7 8
3 4

### 出力例 1

8
(A_1, A_2, A_3) = ((10,20,30), (7), (5,6, 7,8))
です。
A_3 = (5, 6, 7, 8)
であり、
A_{3,4} = 8
なので
8
を出力してください。

### 入力例 2

4
2 9 1
3 8 2 6
1 5
2 4 3
2 2

### 出力例 2

2
A_2 = (8, 2, 6)
なので、
A_{2,2} = 2
です。

### 入力例 3

1
5 100 200 300 400 500
1 5

### 出力例 3

500

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **✅ AC** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 239 ms |
| 提出リンク | [#75629486](https://atcoder.jp/contests/abc457/submissions/75629486) |

<details><summary>提出コード</summary>

```python
N =int(input())
L = [list(map(int, input().split())) for _ in range(N)]
X ,Y= map(int,input().split())


    
    

print(L[X-1][Y])


```

</details>

---

### 解説

## アルゴリズム解説

この問題は、与えられたN個の数列をメモリ上に格納し、指定されたインデックスの要素を効率的に取り出すことを求めています。

1.  **データの読み込みと格納**:
    まず、N個の数列A\_1, A\_2, ..., A\_Nをすべて読み込み、Pythonのリストのリスト（二次元配列）として格納します。
    各数列A\_iは、まずその長さL\_iが与えられ、その後にL\_i個の要素 A\_{i,1}, A\_{i,2}, ..., A\_{i,L\_i} が続きます。この入力形式をそのままリストとして読み込むと、各数列は `[L_i, A_{i,1}, A_{i,2}, ..., A_{i,L_i}]` という構造のリストになります。これをN個集めて `L = [[L_1, ...], [L_2, ...], ..., [L_N, ...]]` のように格納します。

2.  **要素の検索**:
    N個の数列を格納した後、目的の要素を指定するインデックス `X` と `Y` が与えられます。
    問題文の `A_{X,Y}` は、`X` 番目の数列の `Y` 番目の要素を意味します。
    Pythonのリストは0-indexed（インデックスが0から始まる）であるため、
    - `X` 番目の数列は `L[X-1]` でアクセスできます。
    - `Y` 番目の要素は、各数列のリストにおいて、`L_i` がインデックス0に格納されていることを考慮すると、`A_{i,Y}` はインデックス `Y` に格納されています。例えば、`A_{i,1}` はインデックス1、`A_{i,2}` はインデックス2、といった具合です。
    したがって、`A_{X,Y}` の値は `L[X-1][Y]` として直接アクセスし、出力すればよいことになります。

## 計算量

*   **時間計算量**:
    *   `N` の読み込み: O(1)
    *   N個の数列の読み込み: 各数列の長さ `L_i` と要素の総数を `SUM_L` とします。各行の読み込み（`input().split()`）、整数への変換（`map(int, ...)`）、リスト化（`list(...)`）は、その行の要素数に比例します。各行は `L_i + 1` 個の要素（長さ `L_i` と `L_i` 個の数値）を持つため、全体の読み込みにかかる時間は `O(SUM_L)` となります。
    *   `X, Y` の読み込み: O(1)
    *   指定された要素へのアクセス: O(1) (リストのインデックスアクセスは定数時間)
    *   総合して、時間計算量は **O(N + SUM_L)** となります。制約上 `SUM_L` が支配的であるため、実質的に **O(SUM_L)** とみなせます。

*   **空間計算量**:
    *   N個の数列を格納するために必要なメモリです。各数列 `A_i` は `L_i` 個の要素を持ちますが、入力形式に従って長さ `L_i` もリストに含めるため、`L_i + 1` 個の整数が格納されます。
    *   `L_i` の総和が `SUM_L` であるため、全体で `O(SUM_L)` 個の整数を格納するメモリが必要です。
    *   したがって、空間計算量は **O(SUM_L)** となります。

制約 (N <= 2e5, SUM_L <= 2e5) から、この計算量であれば十分高速に処理が完了し、メモリも問題ありません。

## Python 模範解答

```python
# N個の数列の総数を読み込む
N = int(input())

# N個の数列を格納するためのリストのリスト（二次元配列）
# 各数列は、その長さL_iと、それに続くL_i個の要素からなるリストとして読み込まれる
# 例: "3 10 20 30" は [3, 10, 20, 30] となる
# 最終的に L = [[L_1, A_1_1, ...], [L_2, A_2_1, ...], ...] のような構造になる
L_arrays = []
for _ in range(N):
    # 各行を読み込み、スペースで分割し、整数に変換してリストに追加
    L_arrays.append(list(map(int, input().split())))

# 検索対象の数列のインデックスXと、その数列内の要素のインデックスYを読み込む
# 問題文は1-indexed (X, Y) だが、Pythonは0-indexed
X, Y = map(int, input().split())

# A_{X,Y} の値を出力する
# Pythonのリストは0-indexedなので、X番目の数列は L_arrays[X-1]
# 問題文の A_{X,Y} は、L_arrays[X-1] のリストにおいてインデックス Y の位置にある
# 例: L_arrays[X-1][0] は長さL_X, L_arrays[X-1][1] は A_{X,1}, ..., L_arrays[X-1][Y] は A_{X,Y}
print(L_arrays[X - 1][Y])

```

## 実装のポイント

*   **0-indexed と 1-indexed の対応**:
    *   問題文では数列の番号 `X` も要素の番号 `Y` も1から始まる（1-indexed）形式 `A_{X,Y}` で与えられます。
    *   一方、Pythonのリストのインデックスは0から始まります（0-indexed）。
    *   したがって、`X` 番目の数列にアクセスするには `L_arrays[X-1]` を使用します。
    *   `Y` 番目の要素にアクセスする際、入力形式 `L_i A_{i,1} A_{i,2} ... A_{i,L_i}` を考慮する必要があります。各行が `list(map(int, input().split()))` によって読み込まれると、例えば `3 10 20 30` は `[3, 10, 20, 30]` というリストになります。このとき、リストのインデックスと問題文の要素番号は以下のようになります。
        *   `[0]` 番目: 長さ `L_i` （この例では `3`）
        *   `[1]` 番目: `A_{i,1}` （この例では `10`）
        *   `[2]` 番目: `A_{i,2}` （この例では `20`）
        *   `[3]` 番目: `A_{i,3}` （この例では `30`）
    *   このように、`A_{i,Y}` はリストのインデックス `Y` に直接対応します。したがって、`A_{X,Y}` は `L_arrays[X-1][Y]` でアクセスできます。

*   **入力形式の正確な理解**:
    各数列は「長さ L_i」の後に「L_i 個の要素」が続くという形式で与えられます。Pythonの `list(map(int, input().split()))` はこの行全体を一つのリストとして処理するため、`L_i` もリストの先頭要素として含まれます。この特性が、`A_{X,Y}` を `L_arrays[X-1][Y]` で直接アクセスできる理由です。もし `L_i` を読み飛ばして要素だけをリストに入れる実装をする場合は、`L_arrays[X-1][Y-1]` のように `Y` も `Y-1` に調整する必要がありますが、この問題では現在の実装が最も自然で簡潔です。

*   **効率的な入出力**:
    NやSUM_Lが大きいため、`input()` 関数を複数回呼び出すことになります。Pythonの標準的な `input()` は競技プログラミングにおいてはやや遅い場合がありますが、この問題の制約では特に問題になりません。より高速な入出力を求める場合は `sys.stdin.readline` を使用することもありますが、今回は不要です。

---

## 問題C: C - Long SequenceEditorial

### 問題文

整数
N,K
と
N
個の整数列
A_1,A_2,\ldots,A_N
、長さ
N
の整数列
C=(C_1,C_2,\ldots,C_N)
が与えられます。整数列
A_i
の長さは
L_i
であり
A_i=(A_{i,1},A_{i,2},\ldots,A_{i,L_i})
です。また、
\displaystyle 1\le K\le \sum_{i=1}^N C_iL_i
が保証されます。
A
と
C
から以下の手順で整数列
B=(B_1,B_2,\ldots,B_{\sum_{i=1}^N C_iL_i})
を構成します。
B
を長さ
0
の整数列とする。
i=1,2,\ldots,N
の順に以下の操作を行う：
B
の末尾に
A_i
を連結させる操作を
C_i
回行う。
B_K
の値を求めてください。

### 制約

1\le N
1\le L_i
\displaystyle \sum_{i=1}^N L_i\le 2\times 10^5
1\le A_{i,j} \le 10^9
(1\le j\le L_i)
1\le C_i\le 10^9
\displaystyle 1\le K\le \sum_{i=1}^N C_iL_i
入力される値は全て整数

### 入力

入力は以下の形式で標準入力から与えられる。
N
K
L_1
A_{1,1}
A_{1,2}
\ldots
A_{1,L_1}
L_2
A_{2,1}
A_{2,2}
\ldots
A_{2,L_2}
\vdots
L_N
A_{N,1}
A_{N,2}
\ldots
A_{N,L_N}
C_1
C_2
\ldots
C_N

### 出力

答えを出力せよ。

### 入力例 1

3 9
3 1 3 2
1 3
2 4 3
1 3 2

### 出力例 1

4
B
は以下のように構成されます：
B=()
とする。
B
の末尾に
A_1
を連結させる操作を
1
回行う。
B=(1,3,2)
となる。
B
の末尾に
A_2
を連結させる操作を
3
回行う。
B=(1,3,2,3,3,3)
となる。
B
の末尾に
A_3
を連結させる操作を
2
回行う。
B=(1,3,2,3,3,3,4,3,4,3)
となる。
B=(1,3,2,3,3,3,4,3,4,3)
に対し
B_9=4
なので
4
を出力してください。

### 入力例 2

3 1
1 7
1 111
1 5
1 100 10000

### 出力例 2

7

### 入力例 3

3 3163812
5 1 2 3 4 5
4 9 8 7 6
2 10 11
87043 908415 9814

### 出力例 3

9

---

### 提出結果

| 項目 | 内容 |
|------|------|
| 提出結果 | **❌ RE** |
| 言語 | Python (PyPy 3.11-v7.3.20) |
| 実行時間 | 874 ms |
| 提出リンク | [#75668684](https://atcoder.jp/contests/abc457/submissions/75668684) |

<details><summary>提出コード</summary>

```python
from collections import deque
import itertools

N,K =map(int,input().split())
A = [deque(map(int, input().split())) for _ in range(N)]
C = list(map(int,input().split()))

B = []
count = 0 

for row in A:
  if row:
    row.popleft()
  
  
D = []
for row in A:
  if len(D) <= K:
    D.append(list(row))
  else:
    break



count = 0
for row in D:
  if len(B) <= K:
    B.append(row * C[count])
  else:
    break
  if count <= N:
    count += 1

new_B = list(itertools.chain.from_iterable(B))

import itertools

new_list = list(itertools.chain.from_iterable(B))
print(new_list[K-1])


```

</details>

---

### 解説

## RE の原因分析

提出されたコードの Runtime Error (RE) の主な原因は以下の通りです。

1.  **入力の読み込み間違い**:
    問題文の入力形式は、各整数列 $A_i$ の前にその長さ $L_i$ が別の行として与えられます。
    ```
    L_1
    A_{1,1} A_{1,2} ... A_{1,L_1}
    L_2
    A_{2,1} A_{2,2} ... A_{2,L_2}
    ...
    ```
    しかし、提出コードでは `A = [deque(map(int, input().split())) for _ in range(N)]` のように記述されています。この記述だと、`for _ in range(N)` のループ内で `input()` が `N` 回だけ呼ばれることになります。
    これにより、以下のようになります。
    *   1回目の `input()` は `L_1` の行を読み込み、`A[0]` に `deque([L_1])` が格納されます。
    *   2回目の `input()` は `L_2` の行を読み込み、`A[1]` に `deque([L_2])` が格納されます。
    *   ...
    *   $N$ 回目の `input()` は `L_N` の行を読み込み、`A[N-1]` に `deque([L_N])` が格納されます。
    結果として、`A_{1,j}, \ldots, A_{N,j}` の実際の要素列は全く読み込まれず、スキップされてしまいます。`A` には各列の長さ情報しか格納されないことになります。この時点で、以降のすべての処理が間違ったデータに対して行われることになります。

2.  **`C` のインデックスアクセスエラー (IndexError)**:
    ```python
    count = 0
    for row in D:
      # ...
      B.append(row * C[count]) # ここで C[count] にアクセス
      # ...
      if count <= N: # ループ条件が不適切
        count += 1
    ```
    `C` は長さ `N` のリストであり、有効なインデックスは `0` から `N-1` です。
    `if count <= N:` の条件により、`count` が `N` になった場合でも `count += 1` が実行され、`count` が `N+1` になる可能性があります（実際にはループは `N` 回までなので `N` に達する）。
    `for row in D` は `N` 回ループするため、`count` は `0, 1, ..., N-1` となります。`N-1` のループで `count` は `N` になり、次のループで `C[N]` にアクセスしようとして `IndexError` が発生します。
    （もし `D` の要素数が `N` より少ない場合は `IndexError` が発生しない可能性もありますが、基本的には `N` 回繰り返されます）

3.  **非効率なデータ構造とアルゴリズム**:
    問題の制約では、$C_i$ や $L_i$ が最大 $10^9$ になる可能性があります。このため、構築される整数列 $B$ の合計長さ $\sum C_i L_i$ は最大で $N \times 10^9 \times 10^9 \approx 2 \times 10^{14}$ と非常に大きくなる可能性があります。
    提出コードは、`B.append(row * C[count])` のように、実際に `B` に要素を格納しようとしています。これは、たとえ要素が `L_i` だけであったとしても、メモリ上に巨大なリストを生成しようとするため、**メモリ制限超過 (MLE)** または処理時間超過 (TLE) となります。
    `deque` や `itertools.chain.from_iterable` を使用している点も、この問題の解法としては適切ではありません。問題は特定のインデックスの値を求めるだけで、全体の配列を構築する必要はありません。

これらの問題、特に「入力の読み込み間違い」と「`C` のインデックスアクセスエラー」が直接的に RE を引き起こす原因と考えられます。

## 正解アルゴリズム

この問題は、巨大な配列 $B$ を実際に構築することなく、$K$ 番目の要素を効率的に見つける「シミュレーション」または「座標圧縮的なアプローチ」で解く必要があります。

1.  **何を求めているか**:
    $B$ は $A_1$ を $C_1$ 回、次に $A_2$ を $C_2$ 回、...、$A_N$ を $C_N$ 回連結して作られる配列です。この $B$ の $K$ 番目の要素 ($B_K$) を求めます。$K$ は1-indexedです。

2.  **どの手法で解くべきか**:
    *   $A_i$ の長さは $L_i$ です。
    *   $A_i$ を $C_i$ 回連結すると、そのブロックの合計長さは $L_i \times C_i$ となります。
    *   $N$ 個の $A_i$ ブロックを順に見ていき、現在までに連結された要素の総数（累積長）を管理します。
    *   現在のブロック（$A_i$ を $C_i$ 回連結したもの）の長さが、求めたいインデックス $K$ を含むかどうかを判定します。

    具体的な手順：
    1.  `current_length = 0` (これまでのブロックの合計長さ) を初期化します。`K` が1-indexedなので、`current_length` も1-indexedで比較できるよう、ブロックの長さを足し合わせていくことで対応します。
    2.  $i=0$ から $N-1$ までループします。
        a.  $A_i$ の長さ $L_i$ と、その要素リスト $A_{i\_elements}$ を取得します。
        b.  $A_i$ を $C_i$ 回繰り返したブロックの合計長さを `block_length = L_i \times C_i` として計算します。この値は非常に大きくなる可能性がありますが、Pythonの整数型は多倍長整数をサポートするためオーバーフローの心配はありません。
        c.  もし $K \le current\_length + block\_length$ であれば、$K$ 番目の要素は現在の $A_i$ のブロック内にあります。
            i.  このブロック内での $K$ の相対位置を計算します。$K$ は1-indexedなので、`relative_K = K - current_length` となります。
            ii. $A_i$ は $L_i$ 個の要素からなるため、`relative_K` が $A_i$ のどの要素に対応するかは、`(relative_K - 1) % L_i` で求まります（Pythonのリストは0-indexedなので `relative_K - 1` とします）。
            iii. 求める値は `A_i_elements[(relative_K - 1) % L_i]` です。この値を出力してプログラムを終了します。
        d.  もし $K$ が現在のブロックに含まれない場合は、`current_length` に `block_length` を加算し、次のブロックへ進みます。

## 計算量

*   **時間計算量**: $O(N + \sum L_i)$
    *   入力の読み込み: 各 $L_i$ と $A_i$ の要素を読み込むのに $\sum L_i$ の時間がかかります。$C$ の読み込みは $O(N)$。合計で $O(N + \sum L_i)$。
    *   探索ループ: $N$ 個のブロックを最大で $N$ 回ループします。各ループ内での計算（乗算、加算、比較）は定数時間です。したがって、探索に $O(N)$ の時間がかかります。
    *   全体の時間計算量は $O(N + \sum L_i)$ となります。制約 ($N \le 2 \times 10^5, \sum L_i \le 2 \times 10^5$) を満たし、十分高速です。

*   **空間計算量**: $O(N + \sum L_i)$
    *   $A\_list$: 各 $A_i$ の要素をリストとして格納するため、合計 $\sum L_i$ 個の整数を保持します。
    *   $C$: $N$ 個の整数を格納します。
    *   全体の空間計算量は $O(N + \sum L_i)$ となります。これも制約内でメモリに収まります。

## Python 正解模範解答

```python
import sys

def solve():
    # N, K を読み込む
    N, K = map(int, sys.stdin.readline().split())

    # A_i の各列の長さ L_i と要素 A_i_elements を読み込み、タプルのリストとして保存する
    # A_list は [(L_1, [A_1,1, ..., A_1,L_1]), (L_2, [...]), ...] の形式
    A_list = []
    for _ in range(N):
        L_i = int(sys.stdin.readline()) # L_i を読み込む
        A_i_elements = list(map(int, sys.stdin.readline().split())) # A_i の要素を読み込む
        A_list.append((L_i, A_i_elements))

    # C を読み込む
    C = list(map(int, sys.stdin.readline().split()))

    # これまでに連結されたブロックの合計長さ (1-indexedで K と比較するため、累積長も1-indexedと考える)
    current_length = 0 

    # 各A_iブロックを順番に処理する
    for i in range(N):
        L_i, A_i_elements = A_list[i]
        C_i = C[i]

        # 現在の A_i ブロックが B に連結される合計の長さ
        # C_i * L_i は非常に大きくなる可能性がある (10^9 * 10^9 = 10^18)
        # Python の int は多倍長整数なので、オーバーフローの心配はない
        block_length = L_i * C_i

        # もし K が現在のブロックの範囲内にある場合
        # K は 1-indexed で考える
        if K <= current_length + block_length:
            # 現在のブロックの先頭からの K の相対的な位置 (1-indexed)
            # 例: current_length=3, K=5 の場合, relative_K = 5 - 3 = 2
            # これはブロックの2番目の要素を指す
            relative_K = K - current_length

            # A_i_elements のどのインデックスの要素が必要かを計算する (0-indexed)
            # A_i = [x, y, z], L_i = 3 の場合
            # relative_K = 1 (ブロックの1番目) -> (1-1)%3 = 0 -> A_i_elements[0]
            # relative_K = 2 (ブロックの2番目) -> (2-1)%3 = 1 -> A_i_elements[1]
            # relative_K = 3 (ブロックの3番目) -> (3-1)%3 = 2 -> A_i_elements[2]
            # relative_K = 4 (ブロックの4番目, 2回目の繰り返し1番目) -> (4-1)%3 = 0 -> A_i_elements[0]
            target_index_in_A = (relative_K - 1) % L_i

            # 該当する要素を出力し、プログラムを終了する
            print(A_i_elements[target_index_in_A])
            return 
        
        # K が現在のブロックに含まれない場合は、累積長を更新して次のブロックへ
        current_length += block_length

# 関数を呼び出す
solve()

```

## 実装のポイント

*   **正しい入力の読み込み**: `L_i` と `A_i` の要素は別々の行で与えられるため、`int(sys.stdin.readline())` で `L_i` を読み込み、その直後に `list(map(int, sys.stdin.readline().split()))` で $A_i$ の要素を読み込む必要があります。`sys.stdin.readline()` は `input()` よりも高速な読み込み手段です。
*   **多倍長整数の利用**: $C_i \times L_i$ は

---
